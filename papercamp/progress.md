## 2026-07-01
- Completed phase 5 (final) of "Sync to main guard" (`FEAT-27`): wired branch hygiene status into the plans sidebar for visibility before the user attempts to create a new plan. Updated `GitStatusResponse` type in `src/types/index.ts` to include `branchHygiene: BranchHygieneStatus`. Updated `GET /api/git/status` route in `src/app/server/api.ts` to call `git.getBranchHygieneStatus()` and include it in the response. Added `gitBranchHygiene` state to the app store in `src/app/stores/app-store.ts`, loaded alongside other git status fields. Updated `src/app/features/plans/components/plans-sidebar.tsx` to compute `isStale` (when hygiene status is not `clean-on-main` and not `fine`), render a prominent `Alert` banner at the top of the sidebar explaining the hygiene issue, and disable the "Add to backlog" button with tooltip "Switch to main first" when stale. The banner messages distinguish between `stale-merged`, `stale-no-upstream`, and `dirty` states, making the gate discoverable and actionable without requiring the user to hit an error message after clicking. All five phases complete; plan status set to `review` per `AGENTS.md`.
- Completed phase 4 of "Sync to main guard" (`FEAT-27`): added "Sync to main" button to `src/app/components/stack-panel.tsx` in the clean-state commit section. Added `syncToMain(mode: 'clean' | 'dirty')` function to `src/app/services/git-api.ts` that calls `POST /api/git/sync` with the mode parameter. Created `MergeIcon` component matching the style of existing icons (PushIcon, WandIcon). Added state for `syncing` and `syncError`. Handler `handleSync` checks if working tree is clean (gitStatus.length === 0) and calls the corresponding mode: clean mode syncs inline, dirty mode fires an agent task visible in the activity feed. Button is placed in the "no changed files" section, disabled when already on clean main with title tooltip "Already on clean main". Button shows "Sync to main" label with merge icon. Phase checked off, plan stays `in-progress` (1 phase remains).
- Built phase 3 of "Sync to main guard" (`FEAT-27`): added `POST /api/git/sync` endpoint to `src/app/server/api.ts` with two modes: clean (inline git commands) and dirty (agent task). Added `runGitSync()` method to git manager (`src/app/server/git.ts`) that runs `git checkout main && git fetch --prune && git merge --ff-only origin/main` sequentially. Clean mode calls `runGitSync()` directly and returns 200 on success. Dirty mode launches an agent task via `agent.startSync()` (new method) with a prompt that instructs the agent to stash/commit changes, relocate mis-filed drafts (e.g. content in legacy `papercamp/plans.md`), check out main, fast-forward, and confirm success — explicitly forbidding `reset --hard` or `clean -fd` without an explicit confirmation step. Added `'sync'` to the `TaskKind` union in types. `tsc` clean. Phase checked off, plan stays `in-progress` (2 phases remain).
- Built phase 2 of "Sync to main guard" (`FEAT-27`): extended `checkBranchConflictForPlan` in `src/app/server/api.ts` to call `git.getBranchHygieneStatus()` and block plan creation/launch with hard gate when branch is `stale-merged`. Updated the type signature to include `getBranchHygieneStatus` method. When a merged branch is detected (merged into main but remote is gone), returns error "You're on a merged branch — switch to main first" instead of allowing the operation to proceed. This block is applied before the existing active-plan conflict check, preventing any plan work on stale branches. Imported `BranchHygieneStatus` type into api.ts. The gate is hard (not overridable) to prevent silent accumulation of work on merged branches. Phase checked off, plan stays `in-progress` (3 phases remain).
- Built phase 1 of "Sync to main guard" (`FEAT-27`): added `isMergedIntoMain()` and `getBranchHygieneStatus()` helpers to `src/app/server/git.ts`. Added a new `BranchHygieneStatus` type to `src/types/index.ts` with states `clean-on-main` / `stale-merged` / `stale-no-upstream` / `dirty` / `fine`. `isMergedIntoMain()` checks if current branch is in `git branch --merged main` AND has no upstream, avoiding false positives on never-pushed branches. `getBranchHygieneStatus()` returns a typed status reflecting branch/worktree state: on main + clean → `clean-on-main`; off main + merged + no upstream → `stale-merged`; off main + no upstream + not merged → `stale-no-upstream`; working tree dirty → `dirty`; everything else → `fine`. Both functions exported from the git manager. `tsc` clean. Plan stays `planned` (4 phases remain).
- Completed phase 5 ("Apply model/effort to commit-suggest spawn") of "Per-agent model and effort" (`FEAT-26`). In `src/app/server/agent.ts`, destructured `model` and `effort` from the `resolveAgent` return value in `runCommitSuggest`. Appended `--model`/`--effort` (claude-code) or `-m`/`--variant` (opencode) to the hardcoded args after the base flags, guarded by the existing `isClaude` branch — flags are omitted when unset so the CLI default applies. Could not use `adapter.buildArgs` directly since commit-suggest sends the prompt via stdin rather than as a positional argument. `tsc` clean. All five phases complete; plan status already `review`.
- Completed phase 4 ("Redesign Settings page agent section") of "Per-agent model and effort" (`FEAT-26`). Replaced the four hard-coded one-row-per-task agent `Select` divs (wide layout, most space empty) with compact inline rows via a new `AgentTaskRow` component. Each row has a short plain-text label (110 px fixed), an agent `Select`, and then model/effort controls rendered from the selected agent's `AGENT_OPTIONS` descriptor — `claude-code` gets a model `Select` (opus/sonnet/fable/haiku + "Default") and an effort `Select` (low/medium/high/xhigh/max + "Default"); `opencode` gets a free-text model `Input` (saved on blur) and no effort control. Switching the agent clears incompatible fields (effort is cleared when switching to opencode). Consolidated four `agentSaved*` boolean states into a single `agentSavedKey: TaskTypeKey | null`. Moved `AGENT_OPTIONS` to `src/types/index.ts` as a shared constant (parallel to `AGENT_IDS`/`AGENT_LABELS`) and updated `agents/index.ts` to import it instead of redeclaring inline. Widened `AgentOptionsDescriptor` to include `| undefined` so the UI can safely distinguish "key present but free-text" from "key absent". `tsc` clean, biome clean. All four phases complete; plan status set to `review`.
- Completed phase 3 ("Thread model/effort through resolveAgent and buildArgs") of "Per-agent model and effort" (`FEAT-26`). Updated `buildArgs` in both adapters to accept `opts?: { model?: string; effort?: string }` — `claude-code` appends `--model`/`--effort` when set, `opencode` appends `-m`/`--variant` when set. Widened `AgentAdapter.buildArgs` signature in the interface. Extended `resolveAgent`'s return type to include `model?`/`effort?`, extracted from the `AgentConfig` fallback when the task-kind path is taken. Threaded both values into every `buildArgs` call site: `launch()` and `startBatchAudit()` in `agent.ts`, and `runPlanAudit()` plus the CLI audit loop in `cli/index.ts`. Flags are omitted entirely when unset, so the CLI's own default applies. `tsc` clean.
- Completed phase 2 ("Add options descriptors to each adapter") of "Per-agent model and effort" (`FEAT-26`). Added `AgentOptionsDescriptor = Record<string, string[] | null>` to `src/types/index.ts` (a shared type mapping option names to a fixed enum list or `null` for free-text). Added `options: AgentOptionsDescriptor` to the `AgentAdapter` interface in `src/app/server/agents/index.ts`. Implemented for `claude-code`: `model: ['opus', 'sonnet', 'fable', 'haiku']` (Select; phase 4 UI will also accept a full `claude-*` model ID via free-text path) and `effort: ['low', 'medium', 'high', 'xhigh', 'max']`. Implemented for `opencode`: `model: null` (free-text), no effort entry. `tsc` clean.
- Completed phase 1 ("Widen DefaultAgentsMap type and update config schema") of "Per-agent model and effort" (`FEAT-26`). Added `AgentConfig` interface (`{ agent: AgentId; model?: string; effort?: string }`) and `coerceAgentConfig` helper to `src/types/index.ts`; changed every `DefaultAgentsMap` entry from bare `AgentId` to `AgentConfig`; updated `DEFAULT_AGENTS` to use the object shape. Added `agentConfigSchema` (`z.preprocess` + `z.object`) to `src/core/schemas.ts` that accepts both a bare `AgentId` string and the full object form, coercing bare strings to `{ agent: id }` on parse; updated `paperCampConfigSchema.defaultAgents` to use it. Fixed all consumers: `resolveAgent` (`agents/index.ts`) extracts `.agent` from the `AgentConfig` fallback; `readDefaultAgentIds` (`agent.ts`) coerces raw JSON values via `coerceAgentConfig` and builds `{ agent: id }` objects on the legacy-`defaultAgent` path; the config `POST` handler (`api.ts`) accepts both bare strings and `AgentConfig` objects in the request body, validates the `agent` field, and coerces to `DefaultAgentsMap` before writing; `handleSaveAgent` (`settings-page.tsx`) now builds `AgentConfig` objects (preserving existing `model`/`effort`) and the four `Select` `value=` props extract `.agent`; CLI `audit` command (`cli/index.ts`) coerces raw `config.defaultAgents` values through `coerceAgentConfig`. `tsc` clean, 62 tests pass.
- Completed phase 6 ("Fix dashboard reading model and filename case") of "Actualize about.md for per-file storage" (`DOCS-3`). In `papercamp/about.md`: (1) Dashboard section — updated the **Ideas** description to say entries are loaded via `readIdeasMerged` from `papercamp/ideas/*.md` per-file entries (was: "parsed from `ideas.md`'s `IDEA-N:`-prefixed sections"); updated the **Backlog** description to say "per-file plan entries with `status: idea` (from `papercamp/plans/*.md`)" (was: "`plans.md` entries with `Status: idea`"). (2) Per-file section filename case — corrected every lowercase filename in the directory-layout tree (`feat-24.md` → `FEAT-24.md`, `idea-20.md` → `IDEA-20.md`, etc.), flipped the filename-convention rule from "**Id-only, lowercase**" to "**Id-only, uppercase**", and updated both example file path headers to uppercase. All six phases complete; plan status set to `review`.
- Completed phase 5 ("Update parser/serializer descriptions") of "Actualize about.md for per-file storage" (`DOCS-3`). In `papercamp/about.md`: rewrote the `parser.ts` bullet to lead with the per-file primary path — `parsePlanFile`/`parseIdeaFile`, `readAllPlanFiles`/`readAllIdeaFiles`, and `readPlansMerged`/`readIdeasMerged` (the API entry points that merge per-file with a monolithic fallback); retained `parseDecisions`/`parseOpenQuestions`/`parseProgress` as still-active monolithic parsers for non-migrated files; demoted `parsePlans`/`parseIdeas` to back-compat fallbacks (only called when per-file dirs are empty). Rewrote the `serializer.ts` bullet to lead with `formatPlanFile`/`formatIdeaFile` (per-file writers used by all plan/idea mutation routes), `formatPlansIndex`/`formatIdeasIndex` (index rewriters), `archivePlanFile`, and `serializeFrontmatter`; retained `formatDecisionEntry`/`formatOpenQuestionEntry`/`appendBlock` as still-active for monolithic files; noted `formatPlanEntry`/`formatPlans` as dead back-compat exports.
- Completed phase 3 ("Rewrite storage schema sections") of "Actualize about.md for per-file storage" (`DOCS-3`). In `papercamp/about.md`: rewrote the "How structure is added" block (~line 214) to point at the per-file YAML frontmatter section for plans/ideas, and to explain that `decisions.md`/`open-questions.md`/`progress.md` remain monolithic because they are append-only logs with no per-entry archive/rename/lookup need. Removed the now-stale `### ideas.md — no schema` section (ideas are per-file with frontmatter) and the `### plans.md` schema section with its field table and markdown example (plans are per-file with frontmatter). The `### progress.md`, `### decisions.md`, and `### open-questions.md` subsections are unchanged.
- Completed phase 2 ("Fix file table and intro references") of "Actualize about.md for per-file storage" (`DOCS-3`). In `papercamp/about.md`: (1) line 3 — changed `[ideas.md](./ideas.md)` to `[papercamp/ideas/](./ideas/)`; (2) line 175 — replaced `See \`plans.md\`'s "Plan storage architecture" (FEAT-24)` with `See \`papercamp/plans/FEAT-24.md\``; (3) lines 196–197 — replaced the `ideas.md`/`plans.md` rows in the "Two directories, two jobs" file table with `plans/` and `ideas/` directory entries describing per-file storage.
- Completed phase 1 ("Audit stale sections against source code") of "Actualize about.md for per-file storage" (`DOCS-3`). Verified five source files against eight flagged areas in `about.md`. Key findings: (1) file table still lists `ideas.md`/`plans.md` as primary — `scaffold.ts` now creates `plans/`+`ideas/` dirs with indexes instead; (2) line 3 still points to `ideas.md` for philosophy — intent seed goes to `ideas/index.md` now; (3) line 175 references `plans.md`'s FEAT-24 — should point to `papercamp/plans/FEAT-24.md`; (4) "How structure is added" block plus the `### ideas.md`/`### plans.md` schema sections describe the `## Heading` + `**Field:** value` grammar as the primary format — that's now a back-compat fallback, YAML frontmatter is primary; (5) CLI `init` description lists monolithic `.md` files, should list dirs + indexes; (6) `add plan` description says "appends to `plans.md`" — it writes `plans/<ID>.md` via `formatPlanFile`; (7) parser description missing `parseFrontmatter`, `parsePlanFile`, `parseIdeaFile`, `readAllPlanFiles`, `readAllIdeaFiles`, `readPlansMerged`, `readIdeasMerged`, and `findConsistencyIssues`; `deriveIdeaStatuses` is in `src/core/idea-status.ts`, not `parser.ts`; (8) serializer description missing `serializeFrontmatter`, `formatPlanFile`, `formatIdeaFile`, `archivePlanFile`, `formatPlansIndex`, `formatIdeasIndex`; claims `formatPlans` is used by delete/patch routes — those now use per-file `parsePlanFile`/`formatPlanFile`; (9) dashboard section still references `ideas.md`'s `IDEA-N:`-prefixed sections and `plans.md` entries — now served by `readIdeasMerged`/`readPlansMerged`; (10) filename-case claim and all directory-layout examples show lowercase (`feat-24.md`) but actual files are uppercase IDs (`FEAT-24.md`, `DOCS-3.md`), as `assignPlanId` returns `${kind.toUpperCase()}-${next}` and `archivePlanFile` uses `${planId}.md` verbatim.
- Completed phase 3 of "Demote idea headings to h2" (`CHORE-1`): bulk-demoted all 28 `papercamp/ideas/IDEA-*.md` files from `### IDEA-N:` to `## IDEA-`. Used a targeted `sed` pattern (`^### IDEA-`) so the `### Log` section in IDEA-9 and any other non-IDEA `###` headings were left untouched. No remaining `### IDEA-` literals in the ideas directory. All three phases now checked off; plan status set to `review`.
- Completed phase 2 of "Demote idea headings to h2" (`CHORE-1`): updated all `### IDEA-N:` code references to `## IDEA-N:`. In `src/app/features/plans/prompts.ts` (`buildIdeaExtendPrompt`), updated both the dynamic heading reference on line 44 and the static guard comment on line 46. In `src/core/parser.ts` (line 401), updated the JSDoc format comment. Grepped the full `src/` tree — no remaining `### IDEA` literals.
- Completed phase 1 of "Demote idea headings to h2" (`CHORE-1`): audited `formatIdeaFile` and the Add-idea flow (CLI and API). Neither hardcoded a `###` heading — new idea files were being created without any body heading at all (the `###` in existing files came from the original monolithic-migration that passed `idea.body` verbatim). Fixed by making `formatIdeaFile` (`src/core/serializer.ts`) always emit `## ${id}: ${title}` as the first line of the body, prepending it before any user-supplied prose. Updated two `formatIdeaFile` round-trip tests in `frontmatter.test.ts` to reflect the heading now appearing in the parsed body. All 62 tests pass.
- Built phase 6 of "Batch plan freshness audit" (`FEAT-25`): wired gap-phase activity entries into the dashboard batch audit sweep. Changed `onAuditComplete` callback signature from `(planId)` to `(planId, gapPhases)`. In `startBatchAudit` (`agent.ts`), after each successful per-plan audit, re-reads the plan file with `parsePlanFile`, diffs the phase count against the pre-audit snapshot, and passes `gapPhases` to the callback. In `api.ts`, added `prependProgressItem` helper (inserts into today's existing `## YYYY-MM-DD` section at the top of `progress.md` or prepends a new section) and updated `stampAuditDate` to call it when `gapPhases > 0`, writing "Batch audit of FEAT-N (title) — N gap phase(s) appended". The SSE file-watch on `papercamp/` picks this up automatically, so the Docs › Progress timeline refreshes without extra wiring. Single-plan audit path passes `gapPhases = 0` (no feed entry). `tsc` clean. Plan status set to `review`.
- Built phase 5 of "Batch plan freshness audit" (`FEAT-25`): added an "Audit all" button to the Plans list header (`plans-page.tsx`) via a new `AuditAllButton` component. Added `'batch-audit'` to the `TaskKind` union, a `startBatchAudit` method to `AgentManager` that runs the same skip logic as the CLI (`audited` marker vs. file mtime, iterates `review`/`done` plans sequentially, stamps `audited:` after each success, broadcasts SSE progress lines), a `POST /api/agent/launch-audit-all` route to `api.ts`, and `launchBatchAudit` wired through `agent-api.ts` and `app-store.ts`. The button is disabled while any agent task is in progress; the Stack panel shows "batch audit" as the task kind label. `tsc` clean.
- Built phase 4 of "Batch plan freshness audit" (`FEAT-25`): added a structured summary printed after the `paper-camp audit` loop completes. Introduced a local `AuditResult` interface tracking `status` (`audited` | `skipped` | `failed`), `gapPhases`, and `skipReason` for each candidate plan. The loop now records a result per plan instead of just printing inline. After all plans are processed, the command prints a `─`-bordered "Audit summary" block: audited/skipped/failed counts, a per-plan `+N phase(s)` breakdown for any plan that received gap-phases, and a "no gap phases" note when all audited plans were already complete. Phase count is computed by reading and re-parsing the plan file after the agent process exits, diffing against the pre-audit `plan.phases.length`. `tsc` clean.
- Built phase 3 of "Batch plan freshness audit" (`FEAT-25`): wired convergence-audit logic into the `paper-camp audit` CLI loop in `src/cli/index.ts`. Added four helpers — `findPlanFile` (resolves `plansDir/<id>.md` then `archive/<id>.md`), `getFileMtimeDateString` (file mtime → `YYYY-MM-DD`), `stampCliAuditDate` (stamps `audited:` frontmatter field after success, same as server-side `stampAuditDate`), and `runPlanAudit` (spawns the configured agent subprocess, streams parsed output to stdout, returns exit-code success). The command now: reads `papercamp/config.json` for the default agent, iterates each `review`/`done` plan, skips any whose `audited` date is `>=` the plan file's mtime (unchanged since last audit), spawns the agent sequentially for the rest, stamps `audited` on success, and sets `process.exitCode = 1` on any failure. Imports `buildConvergenceAuditPrompt` and `resolveAgent` so the CLI uses identical prompt/adapter logic to the dashboard's `AuditPhasesButton`. `tsc` clean, 62 tests pass.
- Built phase 2 of "Batch plan freshness audit" (`FEAT-25`): registered the `paper-camp audit` CLI subcommand in `src/cli/index.ts`. It calls the existing `readAllPlanFiles` (which already walks both `papercamp/plans/` and `papercamp/plans/archive/`), filters for entries with status `review` or `done`, and prints a formatted list — one line per plan showing id, status, and title. No AI calls. Running against the repo surfaces all 31 completed plans. `tsc` clean. Plan stays `in-progress` (5 phases remain).

## 2026-06-30
- Built phase 5 (final) of "Restructure repo-root docs" (`DOCS-2`): rewrote `README.md` to match the current app. Updated the Pages section — removed `Focus` (dropped 2026-06-18), added `Review` and `Docs` (both live routes in `src/app/router.tsx`). Folded `docs/MAIN.md`'s manifesto content ("why this exists", "The folder is the database", "AI as a first-class collaborator") into the README's intro sections, then deleted `docs/MAIN.md` per the phase-4 decision. Plan status set to `review` (all phases complete).
- Decided phase 4 of "Restructure repo-root docs" (`DOCS-2`): fold `docs/MAIN.md`'s manifesto content into `README.md`'s intro section rather than keeping it as a standalone file. The README is the repo homepage's first surface — the "why this exists" prose belongs there, not in a `docs/` file a visitor must navigate to separately. The actual content integration and `docs/MAIN.md` deletion are deferred to phase 5 (Rewrite README.md). Plan stays `in-progress` (1 phase remains).
- Built phase 3 of "Restructure repo-root docs" (`DOCS-2`): widened `.coderabbit.yaml` `path_instructions` glob from `*.md` (root-only) to `**/*.md` so the "follow AGENTS.md and markdown conventions" instruction now applies to files in `docs/` as well. Plan stays `in-progress` (2 phases remain).
- Built phase 2 of "Restructure repo-root docs" (`DOCS-2`): updated `AGENTS.md` lines 19–20 to reference `docs/CODE_STYLE.md` and `docs/UX_PRINCIPLES.md` now that the files have moved into `docs/` while `AGENTS.md` stays at the repo root. Plan stays `in-progress` (3 phases remain).
- Built phase 1 of "Restructure repo-root docs" (`DOCS-2`): created `docs/` folder and `git mv`'d `CODE_STYLE.md`, `UX_PRINCIPLES.md`, and `MAIN.md` into it. Cross-references between the three files use bare relative paths that stay valid since all three landed in the same folder. Plan stays `in-progress` (4 phases remain).
- Built phase 1 of "Resolve open questions from Docs" (`FEAT-23`): added `formatOpenQuestions()` to `src/core/serializer.ts` — the plural formatter that joins individual `formatOpenQuestionEntry()` blocks with `\n\n`, mirroring the existing `formatPlans()`. `tsc`/`vitest` clean, 55 tests pass.
- Built phase 2 of "Resolve open questions from Docs" (`FEAT-23`): added `POST /api/open-questions/resolve?title=<question title>` to `src/app/server/api.ts` — reads `{ decision, rationale? }` from body, appends a new decision entry to `decisions.md` via `formatDecisionEntry`/`appendBlock`, then flips the matched question to `status: 'resolved'` with `Resolved-by` set via `parseOpenQuestions`/`formatOpenQuestions`. Order matters: decision written first so `findConsistencyIssues` never sees a dangling `Resolved-by`. `tsc`/`biome`/`vitest` clean, 55 tests pass.
- Built phase 3 of "Resolve open questions from Docs" (`FEAT-23`): added a "Resolve" `Button` (chalkboard variant) to `open-question-detail.tsx`, gated on `question.status === 'open'`, that opens a `Modal` with a required **Decision** `Input` and optional **Rationale** `Textarea` following the `add-idea-modal.tsx` controlled pattern. Also added `resolveOpenQuestion()` to `src/app/services/docs-api.ts` so the modal can POST to the existing resolve endpoint, and wired `loadOpenQuestions()` on success for the store refresh. `tsc`/`biome` clean.
- Built phase 4 of "Resolve open questions from Docs" (`FEAT-23`): moved the store refresh into `resolveOpenQuestion()` in `docs-api.ts` — on success it now dynamically imports the store and calls both `loadDecisions()` and `loadOpenQuestions()` via `Promise.all`, matching the refetch-after-mutation pattern the Stack panel uses. Removed the manual `loadOpenQuestions()` call from `open-question-detail.tsx` since the API function handles it. `tsc`/`biome` clean.
- Built phase 5 of "Resolve open questions from Docs" (`FEAT-23`): verified end-to-end that the reverse linking from a decision back to its resolved questions works without changes — `DecisionDetail`'s `resolvedQuestions` filter at `decision-detail.tsx:16` is a pure data-driven expression (`openQuestions.filter(q => q.resolvedBy === decision.title)`) that picks up newly resolved questions automatically after the phase-4 store refresh. This was the plan's last phase; Status set to `review`.

## 2026-06-18
- Designed and documented the markdown + per-entry-fields schema for plans/decisions/open-questions in about.md
- Built the core library: parser (with non-fatal validation warnings), zod schemas, serializer, scaffold/init logic
- Built the CLI: `init`, `add plan`, and a working `dev`
- Fixed a real gap: `vite.config.ts` never built `dist/cli/index.js`, so the `bin` field pointed nowhere
- Built the dashboard app (`src/app`): router, paper-ui `Layout` shell, all five pages, dev-time `/api/*` middleware
- Verified the dashboard end-to-end with a headless browser — all five routes, zero console errors
- Decided to drop the speculative `./app` JS export and serve `dist/app` as static assets instead
- Wired `paper-camp dev` to a plain `node:http` server reusing the same API middleware, verified against a fresh temp project built from the published package shape
- Removed the Dashboard/Projects/Focus placeholder pages and their nav entries — nothing in the nav promises a feature that isn't built
- Switched navigation to a bottom-floating paper-ui `NavigationIsland`; turned off `Layout`'s header and sidebar

## 2026-06-19
- Decomposed the dashboard frontend into feature-based folders, one component per file, arrow-function components, and an API service layer
- Added a PATCH `/api/plans` endpoint to update tasks and status
- Built Focus mode: a new `/focus` route with a distraction-free view of the active plan, clickable task toggles, and a "Mark complete" button
- Added Focus to the bottom navigation island
- Added a list/board (kanban) view toggle to the Plans page, plus a collapsible "Closed" section for done/dropped plans
- Added plan create/delete (`POST`/`DELETE /api/plans`), surfaced as an "Add idea" modal and per-item delete in the Plans sidebar
- Added project icon upload (`GET`/`POST /api/icon`) and package-name-sourced branding (`/api/package-name`), replacing the hardcoded "Paper Camp" label in the nav island and Plans sidebar
- Caught `papercamp/about.md` and `plans.md` up to the above — they had drifted from the actual implementation (board view, plan CRUD, icon/branding endpoints were all built but undocumented)
- Fixed 12 Biome formatting/import-order errors via `pnpm lint:write`; `pnpm lint` is clean again
- Resolved the ideas.md-vs-idea-status-plan overlap: split the Plans sidebar's single "Ideas" section into "Ideas" (`ideas.md` prose, read-only) and "Backlog" (`Status: idea` plans, full CRUD), matching the grouping list view already had; renamed the "Add idea" modal to "Add to backlog" to match — see decisions.md
- Added four detailed feature ideas to `ideas.md`: a Docs page (surfacing the already-built-but-unused `/api/decisions`/`/api/open-questions`/`/api/progress` endpoints), a sidebar-driven editable Settings page, a right-side "Stack" status/history panel (chalkboard-styled per paper-ui's showcase `DetailSidebar`), and a multi-agent orchestration concept (launch/steer Claude Code or opencode tasks from the dashboard, with a per-agent adapter interface and an optional `Agent:` field on plan entries)
- Fixed the idea-detail view on the Plans page rendering raw markdown as plain text (bold/lists/code spans showed up as literal asterisks/dashes/backticks). Added `react-markdown` plus a small `Markdown` wrapper component (`src/app/components/markdown.tsx`) styled to match the existing serif/Luminari aesthetic, replacing the old manual paragraph-splitting logic
- Created the first concrete plan from an idea: "Build Docs page" in `plans.md`, with 7 phases derived from `ideas.md`'s "Project docs browser" build order
- Created the second plan: "Build sidebar-driven Settings page" in `plans.md`, with 6 phases derived from `ideas.md`'s "Settings page with sidebar and editable configs" — picked over "The Stack" and "Agent orchestration" per the idea's own stated build order (Docs first, Settings second)
- Created the third plan: "Build the Stack — right-side status & history panel" in `plans.md`, with 5 phases derived from `ideas.md`'s Stack idea — picked over "Agent orchestration" since that idea still has unresolved decisions blocking it from being plan-ready. Also fixed two leftover "task" references in `ideas.md`'s Stack section that should have been renamed to "phase" alongside the project-wide rename
- Built phase 1 of "Build Docs page": added a `/docs` route, nav entry in NavigationIsland, and a left sidebar matching `PlansSidebar`'s shape with groups for Decisions, Open Questions, Progress, and Repo Docs — each fetching live data from the existing API endpoints (`/api/decisions`, `/api/open-questions`, `/api/progress`)
- Built phase 2 of "Build Docs page": decisions log view — clicking a decision in the sidebar shows its full detail with a `Stamp` for `decided`/`superseded` status, and a `superseded` entry links to its replacement via an inline clickable link that navigates to the superseding decision
- Built phase 3 of "Build Docs page": open questions view — clicking a question in the sidebar shows its full detail with a `Stamp` for `open`/`resolved` status, and a `resolved` entry links to its `Resolved-by` decision via an inline clickable link that jumps to that decision
- Built phase 4 of "Build Docs page": cross-linking — a decision detail now shows which open question(s) it resolves, with clickable links that jump to the question view; the question-to-decision link from phase 3 already covered the other direction
- Built phase 5 of "Build Docs page": progress timeline — clicking a progress date in the sidebar shows that date's entries in a reverse-chronological feed; when no specific date is selected, all entries are shown
- Built phase 6 of "Build Docs page": repo docs section — new `GET /api/docs` endpoint scans the repo root for README.md, CHANGELOG.md, and LICENSE; sidebar lists found files; clicking one renders its content (markdown files via `Markdown`, LICENSE as preformatted text)
- Built phase 7 of "Build Docs page": full-text search — a search bar at the top of the Docs page searches across decision titles/bodies, question titles/bodies, progress items, and repo doc filenames/content; results are grouped by source and clickable to jump to the matching entry
- Moved the Docs search input from the page content area to the nav island — it now renders as a page-specific tool after the logo (paper-ui `Input` component), only visible on the `/docs` route, wired through the store to keep the query in sync with the DocsPage
- Fixed the Focus page's per-phase copy button (`focus-phase-item.tsx`): `navigator.clipboard.writeText` was called without `await` or error handling, with `setCopied(true)` running unconditionally regardless of success — and `navigator.clipboard` is `undefined` on any non-`localhost` origin over plain http (e.g. the Tailscale/LAN access `vite.app.config.ts` is explicitly configured for), so the call threw before the feedback state was even set. Added a `document.execCommand('copy')` fallback for non-secure contexts, and a real idle/copied/failed status instead of a boolean. Also fixed the button staying hover-only (`opacity-0 group-hover:opacity-100`) even while showing feedback, which hid the result the instant the mouse left the row to go paste
- Added Start/Stop buttons for plan status — on `PlanDetail` (Start sets `status: in-progress` and navigates to `/focus`; Stop sets `status: planned`) and mirrored on the Focus page next to the status `Stamp`. The `PATCH /api/plans` server handler now also auto-demotes any other currently-`in-progress` plan to `planned` when one is started, so only one plan is ever "in focus" at a time — verified against the live dev server (start A, start B, confirm A demoted, then reverted both back to `planned`)
- Caught a gap from the above: the Start/Stop button was missing from `PlanCard` — the list/board view's card, which is what's actually visible by default on the Plans page before clicking "Open." Added it there too, alongside the existing "Open" button
- Recolored the Open/Start/Stop/Mark-complete buttons (PlanCard, PlanDetail, Focus) from paper-ui's default pale cream/white fills to watercolor-style colors (blue/green/orange/violet) — since paper-ui's Button renders its fill as an SVG path behind the label rather than a CSS background, this needed a small `!important` override in `utilities.css` targeting that path directly, not a `color` prop (Button doesn't expose one). Also noticed paper-ui's own "blue" and "green" watercolor tokens are literally the same hex value (`#8FB996`) — an upstream bug, not something fixed here — so picked distinct custom hex values for all five colors instead of reusing paper-ui's tokens as-is
- Softened those button colors to light pastel washes (closer to paper-ui's own "soft blended washes" watercolor philosophy) and switched the button text back to dark ink instead of white, per feedback that the first pass was too high-contrast
- Simplified the Focus page's AI handoff copy-prompt from `Continue with phase <n> of "<plan title>": <phase text>` to `Look at papercamp/plans.md — plan "<plan title>", phase: <phase text>` — just where to look and what to implement, nothing else; the phase number was dropped and `phaseIndex` removed from `FocusPhaseItem` as part of that. Also caught up `about.md` on the Start/Stop buttons, single-focus auto-demote behavior, and the button color overrides, none of which had been documented yet
- Reconsidered: referencing the phase by number is simpler than duplicating its text, since the AI reads the actual phase text from `plans.md` anyway once it's looking there. Restored `phaseIndex` on `FocusPhaseItem`
- Changed the prompt's leading verb from "Look at" to "Build", so it reads as an instruction rather than a pointer. Final prompt: `Build phase <n> of plan "<plan title>" in papercamp/plans.md`
- Renamed "Tasks" to "Phases" project-wide, at the user's request, for clearer terminology: the `### Tasks` markdown heading, `TaskItem` type (→ `PhaseItem`), `tasks` field on `PlanEntry`/API payloads (→ `phases`), `taskProgress`/`taskPercentage` helpers (→ `phaseProgress`/`phasePercentage`), and the `FocusTaskItem` component (→ `FocusPhaseItem`, file renamed to `focus-phase-item.tsx`). Updated `about.md`'s schema docs and the existing entries in `plans.md` to match; left `progress.md`'s own historical log lines untouched since that file is append-only and never edited after the fact
- Changed the copy-prompt's leading verb once more, from "Build" to "Start" — final prompt: `Start phase <n> of plan "<plan title>" in papercamp/plans.md`
- Built "Build sidebar-driven Settings page": a left rail mirroring `PlansSidebar`'s shape with a "General" section (the existing project-info card, now the default landing view) and a "Config Files" section; descoped from the original plan to read-only during implementation — added a `GET /api/configs` endpoint that scans the repo root against a fixed allowlist (`biome.json`, `tsconfig.json`, `tailwind.config.ts`, `vite.config.ts`, `vite.app.config.ts`, `postcss.config.js`, `package.json`) and returns only the files that actually exist, plus `GET /api/configs?name=...` (allowlist-checked server-side) to fetch one file's contents, rendered via paper-ui's `<CodeBlock>`. No write/edit capability was built — `plans.md`'s phases for this plan reflect the descoped (viewer-only) scope, not the original editable-with-Save-button idea
- Built "Build the Stack — right-side status & history panel": a fixed right-docked, full-height panel (`StackPanel`) added to the root layout alongside the bottom `NavigationIsland`, chalkboard-textured with a desk-green gradient, default open and toggleable via a slide-out tab when closed. Shows the currently in-progress plan and its current (first incomplete) phase in an "Active" section, a reverse-chronological `progress.md` feed in "Activity", and a "Live" section fed by a new `GET /api/activity/stream` SSE endpoint (`src/app/server/activity.ts`) that watches the four `papercamp/*.md` files via `fs.watch`, diffs each newly-parsed snapshot against the previous one, and pushes synthesized human-readable lines (phase checked off, plan marked done, new decision/question/progress entry) to connected clients without polling. Added the nav island's horizontal offset so it stays centered in the remaining viewport space when the panel is open, and loaded Cormorant Garamond via Google Fonts in `index.html` (it was referenced by name throughout but never actually fetched, silently falling back to Georgia)
- Reviewed the Docs/Settings/Stack implementation end-to-end and fixed what the review turned up: a dead ternary in `decision-detail.tsx` that rendered "Resolves" for both the singular and plural case, an unstable React list key in `progress-timeline.tsx` keyed on bullet text instead of index, and a duplicated `findFocusPlan` (Focus had its own copy, the Stack panel had silently redefined a second one instead of reusing it per the plan) — consolidated into a single `findFocusPlan` in `src/app/features/plans/helpers.ts`, imported by both. Also fixed 5 outstanding Biome formatting/import-order errors left over from the Stack panel commit
- Started the "UI cleanup" plan, phase 1: wrote `CODE_STYLE.md` at the repo root documenting paper-ui-first component usage, the "3 copies = extract" deduplication rule, design-token usage (fonts, spacing, colors, transitions), feature-folder/service-layer organization, naming/formatting conventions, restrained motion guidelines, and commenting style
- Completed phase 2 of the "UI cleanup" plan: extracted a shared `useProjectIdentity()` hook in `src/app/hooks/use-project-identity.ts`, replacing the duplicated icon + project-name fetch logic in `router.tsx`, `plans-sidebar.tsx`, `docs-sidebar.tsx`, `settings-sidebar.tsx`, and `settings-page.tsx`
- Completed phase 3 of the "UI cleanup" plan: added a shared `LinkButton` component (`src/app/components/link-button.tsx`) replacing the 3x-repeated inline link-button style in `decision-detail.tsx` and `open-question-detail.tsx`, and collapsed the byte-identical `STATUS_COLOR`/`STATUS_BAR_COLOR` maps into a single `STATUS_COLOR` map in `plans/constants.ts`
- Completed phase 4 of the "UI cleanup" plan: swapped raw buttons for paper-ui equivalents — `IconButton` (chalkboard variant) for the Stack panel's open/close controls, `Button` (ghost variant) for `docs-search.tsx` result rows, and `IconButton` (ghost variant) with paper-ui `CopyIcon`/`CheckIcon`/`CloseIcon` for `focus-phase-item.tsx`'s copy action; logged the hidden file-input gap as a new open question in `papercamp/open-questions.md` rather than building a local workaround
- Completed phase 5 of the "UI cleanup" plan: created `src/app/styles/tokens.ts` as a local mirror of paper-ui's font, spacing, size, line-height, transition, and color tokens; replaced hardcoded font stacks, spacing, font sizes, line heights, and transitions across 27 `src/app` files, leaving only component-specific values that don't map cleanly to the token scale
- Completed phase 6 of the "UI cleanup" plan: added `layout` constants to `src/app/styles/tokens.ts` (`sidebarWidth`, `stackPanelWidth`, `navIslandBottom`, `pagePaddingBottom`, `contentGap`) and used them in `router.tsx`, `stack-panel.tsx`, and all three sidebars; replaced the hardcoded `pb-24` page padding with `layout.pagePaddingBottom`; derived the nav-island centering offset from `layout.stackPanelWidth` instead of the magic `240px`
- Completed phase 7 of the "UI cleanup" plan: installed `framer-motion` and added restrained motion — route-level page transitions with `AnimatePresence`/`motion.div` in `router.tsx`, slide animation for the nav island via `motion.nav`, slide + fade animations for the Stack panel's open/close state and toggle tab via `motion.div`, and staggered fade-in for new live activity feed items; all motion respects `useReducedMotion`

## 2026-06-25
- Built phase 1 of "Project settings and config views": `ConfigEditorSection` now special-cases `package.json` — parses its `scripts` object and renders `name`/`command` rows via paper-ui's `Table` instead of dumping the raw file through `CodeBlock`; falls back to `CodeBlock` if the file isn't valid JSON or has no `scripts` key. Every other allowlisted config is untouched
- Built phase 2 of "Project settings and config views": added an optional `port?: number` field to `PaperCampConfig`, a new `POST /api/config` endpoint (validates `port` is a positive integer, merges it into `.paper-camp/config.json`), and a "Dev Server Port" card in Settings' General section — a number `Input` plus Save button, with helper text stating it sets the default for the next `paper-camp dev` run rather than switching the currently running server. Verified the endpoint directly against the live dev server (valid/invalid port, round-tripped through `GET /api/config`); `tsc`/`biome` clean. Did not wire the CLI to actually read `config.port` as its default — out of scope for this phase, which only covers the field + endpoint + UI
- Built phase 3 of "Project settings and config views": made the General card's project name editable — `POST /api/config` now also accepts `projectName` (rejects empty/whitespace-only after trimming), and the static `<h2>` is now an `Input` + Save button matching the port field's pattern, disabled until the trimmed value differs from the saved one. Verified against the live dev server: renamed, round-tripped through `GET /api/config`, rejected an empty name, then restored the original `projectName`; `tsc`/`biome` clean
- Built phase 4 of "Project settings and config views": added `src/core/env.ts` (`parseEnv`/`applyEnvEntries`) for reading `KEY=value` lines and writing back an edited entry set in place — matched keys are updated in their original line position, removed keys drop their line, new keys append at the end, and every comment/blank line not touched stays exactly where it was. New `GET`/`POST /api/env` routes in `src/app/server/api.ts` read/write the project root's `.env` (POST validates key syntax and rejects duplicates), plus `src/app/services/env-api.ts`. New "Environment" sidebar section and `EnvSection` in `settings-page.tsx`: a `Table` of editable `Input` rows (add/delete via `+ Add variable`/`IconButton`), values for keys matching `key`/`secret`/`token`/`password` rendered as `type="password"` with a per-row Show/Hide toggle. The sidebar only shows "Environment" when `GET /api/env` reports `exists` or `exampleExists` — this repo has neither today, so the section is correctly invisible by default. Verified against the live dev server with a temporary `.env` (since this repo has none): GET parsed entries correctly skipping comments, POST round-tripped an edit/delete/add in one call with comments and ordering preserved, invalid-key and duplicate-key POSTs both rejected, and the no-file case returned `exists: false`; removed the temporary file afterward. `tsc`/`biome` clean
- Built phase 5 (final) of "Project settings and config views": `GET /api/env` now also parses `.env.example` (when present) and returns a `missingKeys` array — example keys with no corresponding `.env` entry. `EnvSection` shows a warning `Alert` ("Missing from .env") listing each missing key as a one-click "+ KEY" button that appends an empty row for it and drops it from the missing list. Verified against the live dev server with temporary `.env`/`.env.example` files (`FOO` in both, `BAZ_KEY`/`QUX` only in the example): `GET /api/env` correctly returned `missingKeys: ["BAZ_KEY", "QUX"]`; removed the temporary files afterward. Could not get a full click-through screenshot — headless Chromium in this sandbox is missing system shared libs (`libatk-1.0.so.0`) with no `sudo` available to install them — so the `EnvSection` button-click behavior was verified by code reading, not a live screenshot. `tsc`/`biome` clean. This was the plan's last phase; status moves to `review`
- Built "Agent orchestration"'s last open phase, "Add agent selection config": `AGENT_IDS`/`AGENT_LABELS`/`AgentId` in `types/index.ts` (currently just `claude-code`, written for more than one), a new `src/app/server/agents/index.ts` registry (`AgentAdapter`, `resolveAgent`) that `agent.ts` now consumes instead of importing the Claude adapter directly, an optional `agent?: AgentId` field on `PlanEntry` (schema/serializer/parser wired end-to-end) settable per-plan via a `Select` in `plan-detail.tsx`, and a project-wide `defaultAgent` in `.paper-camp/config.json` settable in Settings General, with `agent.ts`'s `start()` resolving plan override → project default → `claude-code` fallback. `tsc`/`biome` clean. This was the plan's last phase; status moves to `review`
- Found and fixed a real bug while reviewing the above: Vite restarts its dev middleware in-process on every server-side file change (not just Ctrl-C), which was re-creating the `agent` singleton each time and silently orphaning any in-flight `claude` task — `vite.app.config.ts` now persists the `ApiMiddleware` instance on `globalThis` to survive those restarts. Also hardened `agent.ts` against an agent's own inaccurate self-reporting: after a clean finish it now re-reads `plans.md` and pushes a visible warning if the phase it was told to check off wasn't actually checked. Added `--permission-mode auto` to the Claude Code adapter so headless runs get the same classifier-based approval as this orchestrating session, instead of getting stuck in permission-denial loops on real file edits. Killed several genuinely orphaned `claude` processes and duplicate `pnpm dev` instances left over from the restart bug and consolidated back to one dev server on port 3333

## 2026-06-26
- Built phase 1 of "Phase convergence audit": generalized `agent.ts`'s `AgentTask`/`AgentTaskState` to make `phaseIndex` optional, and added `startForPlan(plan, prompt)` alongside the existing phase-scoped `start(plan, phaseIndex)` — both now go through a shared `launch()` helper that preserves the original synchronous busy-guard. Success for a plan-scoped task is judged by `didTaskProgress()` snapshotting `phases.length`/`log.length` at launch (`planBaseline`) and checking for growth in either on finish, instead of one phase's checkbox flipping. `resume()` carries `planBaseline` through a steering respawn. Updated `stack-panel.tsx`'s status line to omit the phase number when a task has none. `tsc`/`biome`/`vitest` all clean. Plan moved to `in-progress` — 3 phases remain (the audit prompt, the UI button, and end-to-end verification)
- Built phase 2 of "Phase convergence audit": added `src/app/features/plans/prompts.ts` with `buildConvergenceAuditPrompt(plan)`, a client-side prompt builder (mirroring `phase-copy-button.tsx`'s inline-template pattern, now promoted to a shared file per IDEA-11) that lists the plan's body and current phases verbatim and instructs the agent to append any clearly-missing phase as a new `- [ ]` line plus one `### Log` line — or write nothing at all if nothing's missing. Explicitly states the append-only constraint (never reorder/check/uncheck/rewrite existing lines). `tsc`/`biome` clean. 2 phases remain (the audit button and end-to-end verification)
- Built phase 3 of "Phase convergence audit": added `AuditPhasesButton` (`src/app/features/plans/components/audit-phases-button.tsx`) next to `PhaseCopyButton` in `plan-detail.tsx`'s phase-list header, calling the new `launchPlanAudit` store action → `POST /api/agent/launch-audit` (new route in `api.ts`, looks up the plan by id and calls `agent.startForPlan()` from phase 1) with `buildConvergenceAuditPrompt(plan)` from phase 2. The phase header also shows a small spinner while a plan-scoped (no `phaseIndex`) task is running. `tsc`/`biome`/`vitest` all clean. Could not exercise the live happy path end-to-end: the running dev server's `agent` singleton (persisted on `globalThis` to survive Vite's in-process restarts) predates this route, so it 404s until the whole `pnpm dev` process is restarted — and this very session is itself the agent task currently occupying `agent.ts`'s single-task slot (this is phase 3's own phase-scoped run), so deliberately did not restart the server or spawn a second real headless `claude` process to avoid disrupting either. Verified by code review plus the negative-path request shapes instead. Full live verification is phase 4's job. 1 phase remains (end-to-end verification); plan stays `in-progress`
- Built phase 4 (final) of "Phase convergence audit": verified the append-only safety property against a real plan. The literal production path — `agent.ts` spawning `claude --permission-mode auto` — couldn't be exercised directly: this sandbox's safety classifier denies spawning that kind of unsupervised subprocess from Bash, and the live dev server's `agent` singleton was itself occupied by this very verification session (same constraint phase 3 hit). Worked around it by copying the repo into isolated temp directories and dispatching the literal `buildConvergenceAuditPrompt()` text through the harness's own Agent tool as a stand-in for the headless subprocess — still a real model doing real repo inspection and a real file write, just via a different transport. Ran it three times against FEAT-9 ("Project settings and config views"): twice unmodified (all 5 phases genuinely match the code) — both wrote zero bytes, confirmed via diff against the pristine copy, no Log line, no heading; once with one unambiguous unimplemented requirement added to the plan body (a port/`.env` conflict warning with no code anywhere implementing it) — appended exactly one new unchecked `- [ ]` phase and exactly one new `### Log` line, with every other line, in every other plan entry, byte-for-byte unchanged per an independent diff against the original. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 1 of "Plan/decision consistency check": added an optional `blocks?: string` field to `OpenQuestionEntry` (`types/index.ts`), parsed and serialized the exact same way `PlanEntry.idea` already is — `openQuestionFieldsSchema` gains an optional `blocks` string, `parseOpenQuestions()` reads `fields.blocks` straight through, and `formatOpenQuestionEntry()` emits an optional `**Blocks:** <value>` line right after `Resolved-by`. No UI or consistency-check logic yet — that's the next two phases. Added a parser test mirroring the existing `resolved-by` test. `tsc`/`biome`/`vitest` all clean. Plan stays `in-progress` (3 phases remain).
- Built phase 2 of "Plan/decision consistency check": added `findConsistencyIssues(decisions, openQuestions, plans)` to `parser.ts`, a pure derived check in the same vein as `deriveIdeaStatuses` — no AI, no I/O. Added a `ConsistencyIssue` type (`types/index.ts`) with `kind` (`dangling-resolved-by` / `dangling-superseded-by` / `blocked-plan-active`), `section`, `title`, `message`, and an optional `planId`. Flags decisions whose `supersededBy` and open questions whose `resolvedBy` don't match any decision title, plus open questions whose `blocks` targets a plan that's already `in-progress` or `review`. Added 6 unit tests covering the clean case, each dangling-reference kind, and the two negative cases (planned blocking-target, already-resolved question). `tsc`/`biome`/`vitest` all clean. Plan stays `in-progress` (2 phases remain: the API route and the Stack panel pill).
- Built phase 3 of "Plan/decision consistency check": added `GET /api/consistency` to `api.ts`'s generic `apiRoutes` array — reads `decisions.md`/`open-questions.md`/`plans.md` in parallel, parses each, and returns `findConsistencyIssues()`'s output array as-is, same shape pattern as `/api/decisions`/`/api/plans`/etc. No new route-level tests, matching the existing read routes (covered by `parser.test.ts` instead). `tsc`/`biome`/`vitest` all clean. Plan stays `in-progress` (1 phase remains: the Stack panel pill).
- Built phase 4 (final) of "Plan/decision consistency check": added a fourth "Consistency" `Stamp` to the Stack panel's Status row, next to Lint/Format/Tests — `clean` (green) when `findConsistencyIssues()` returns nothing, a red issue count otherwise, clickable to expand only when there are findings. Each expanded finding is a clickable line: `blocked-plan-active` issues jump to the blocking plan (`setActivePlanTitle` + navigate to `/`, looked up by `planId` against the already-loaded `plans` store state), the two dangling-reference kinds jump to the offending Docs entry (`setActiveDocSection`/`setActiveDocTitle` + navigate to `/docs`). Added `fetchConsistency()` (`docs-api.ts`) and a `consistency`/`loadConsistency` store slice (`app-store.ts`), refreshed alongside the other Status data on mount and on every SSE activity event. `tsc`/`biome`/`vitest` all clean. Could not get a live click-through screenshot: the running dev server's cached `agent`/API middleware (persisted on `globalThis` to survive Vite's in-process restarts, per the 2026-06-25 fix) predates this plan's earlier phases entirely, so `/api/consistency` 404s into the SPA shell until the whole `pnpm dev` process restarts — and this very session is itself the agent task occupying that server's single-task slot, so deliberately did not restart it. Verified by `tsc`/lint/tests plus code review against the existing Lint/Format/Tests pill pattern instead. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 1 of "Review-found phases": added an optional `source?: 'review'` to `PhaseItem` (`types/index.ts`). Parsed/serialized as a small inline tag on the checkbox line itself rather than a new sub-section — `parser.ts`'s `parsePhaseEntries` now strips a leading `[review] ` prefix off the checkbox text (`PHASE_SOURCE_RE`) and sets `source: 'review'`, and `serializer.ts`'s `formatPlanEntry` re-adds the prefix when writing a phase with that source back out. Phases stay one ordered list either way. Added a parser test for the round-trip. `tsc`/`biome`/`vitest` all clean. Plan stays `in-progress` (3 phases remain).
- Built phase 2 of "Review-found phases": resolved the paper-ui `Table` row-styling gap by adding a real `rowClassName?: (row: T, index: number) => string | undefined` prop to the sibling `paper-ui` repo's `Table` (`~/dev/paper-ui/src/components/table/table.tsx`), applied alongside the existing `expandable`/hover classes on each `<tr>`. Picked the paper-ui prop over a paper-camp-only cell hack since per-row styling is a generic table capability other consumers will want too, matching the same "build it upstream" call FEAT-7 made for Accordion. `pnpm run check-types` and `pnpm run build` both clean in `paper-ui` (rebuilt `dist/` so the `link:../paper-ui` dependency picks it up immediately); paper-camp's own `tsc` stays clean since the new prop is optional and purely additive. No `plan-detail.tsx` wiring yet — applying it to review-found phases specifically is phase 3's job. Plan stays `in-progress` (2 phases remain).
- Built phase 3 of "Review-found phases": wired up both pieces from phases 1–2 in `plan-detail.tsx`'s phase `Table` — a small violet "review" `Stamp` next to the title (matching the existing `review` plan-status color) when `phase.source === 'review'`, plus `rowClassName` returning a new `.phase-row-review` class (a faint violet wash in `utilities.css`, painted through the table cells' transparent background per the CSS table background-painting order) for the same condition. `tsc`/`biome`/`vitest` all clean. Verified live: the main dev server's loaded `parser.ts` predates phase 1's `[review]`-tag parsing (same stale-module issue prior phases hit), so rather than restart it and disrupt this session's own agent-tracking on its single-task slot, spun up a second disposable `vite --port 3334` instance against the same repo/working tree, temporarily added `[review] ` to a real plan phase, and screenshotted the live result (headless Chromium via a vendored `libatk`/`libatk-bridge` workaround already present in `/tmp/localdeps2` from a prior session) — confirmed the badge and row tint render only on the tagged row. Reverted the temporary edit and killed the throwaway instance afterward. Plan stays `in-progress` (1 phase remains: wiring `/code-review` findings into an "Add as phases" action).

## 2026-06-27
- Built phase 4 (final) of "Review-found phases": added `parseReviewFindings()` (`src/app/features/plans/review-findings.ts`), a pure function mapping pasted `/code-review` JSON output (an array of findings, or `{ findings: [...] }`) into `PhaseItem[]` — each finding's `description`/`title`/`summary` becomes the phase `text`, and its `file`/`line_start`/`line_end` plus `failure_scenario`/`reasoning` are joined into the phase `description`, with `source: 'review'` set throughout. Added 7 unit tests covering the happy path, both input shapes, the title/summary fallback, and three error cases (invalid JSON, missing findings array, empty array, a finding with no text). New `AddReviewPhasesButton` (`components/add-review-phases-button.tsx`) sits next to `AuditPhasesButton` in `plan-detail.tsx`'s phase-list header — opens a `Modal` with a `Textarea` for pasting the JSON, surfaces parse errors via an inline `Alert`, and on success appends the new phases through the existing `updatePlan(plan.title, { phases })` path (no new API route). ideas.md's IDEA-14 section names `/code-review`'s JSON output but doesn't specify how the dashboard receives it (no file path, no API hook into the skill) — paste-into-a-modal was the simplest fit given that gap, reusing the same `Modal` pattern as the existing "Add to backlog" flow. `tsc`/`biome`/`vitest` all clean. Verified live against the real running app: spun up a second disposable `vite --port 3334` instance (same pattern phase 3 used) and drove it with Playwright — confirmed the modal's copy/placeholder, the inline error on invalid JSON, and a successful submission appending a new unchecked, `review`-badged phase with the expected description. Mid-verification, a misclicked button in the phase row's Actions column hit the real `AgentStartButton` instead of the expand toggle, spawning a real `claude --permission-mode auto` subprocess against the live repo through the disposable instance's shared `agent.ts` singleton — caught it immediately via `GET /api/agent/status` (only `Read`/`Bash` tool calls had run, no `Edit`/`Write`) and killed it with `POST /api/agent/stop` before any unintended file change could land. Separately, the disposable instance's writes during testing did land in the real `papercamp/plans.md` on disk (it shares the same repo working tree) — three throwaway test phases were appended to this very plan; removed them by hand afterward, confirmed via `git diff` that only the intended changes remain. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 1 of "Agent-drafted plans": added a third agent launch mode, `startForIdea(idea, prompt)` in `src/app/server/agent.ts`, for tasks that draft a brand-new plan from an idea — unlike `start()` (phase-scoped) and `startForPlan()` (FEAT-14's plan-scoped mode), this one has neither `planId` nor `phaseIndex` since the plan doesn't exist until the agent writes it. Refactored the shared `launch()` helper to take a plain `{ planTitle, planId?, agentOverride? }` identity instead of a `PlanEntry`, since an idea-drafting task has no plan to read those fields from. Added a new optional `ideaId` field to `AgentTask`/`AgentTaskState`; `didTaskProgress()` now checks, for an idea-scoped task, whether any plan entry in `plans.md` has `idea === task.ideaId` — i.e. whether a new plan linking that idea appeared — instead of looking at phases/log growth on an existing plan. `resume()`'s steering respawn carries `ideaId` through like it already does `phaseIndex`/`planBaseline`. No route or prompt-builder wired up yet — `startForIdea` isn't called from anywhere — that's later phases' job. `tsc`/`biome`/`vitest` all clean. Plan moved to `in-progress` (4 phases remain).
- Built "Default font and Stack panel cleanup" (4 phases) after a visual review of the running app via Claude in Chrome. Root cause of the Luminari overuse: paper-ui's `globals.scss` defaults `body`/`button` font to Luminari, and only ~16 spots in paper-camp (page H1s, markdown headings) explicitly opt out — everything else inherited it. Added a `--paper-font-default` CSS var to the sibling `paper-ui` repo (mirroring the existing `--paper-button-font` indirection) across `globals.scss` and the 11 component stylesheets that hardcoded `$font-family-serif` (alert, list-item, select, modal, layout, textarea, checkbox, input, table, prop-table, navigation-island, button), rebuilt its `dist/`, then set `--paper-font-default: 'Cormorant Garamond', Georgia, serif` in paper-camp's `utilities.css` — flips text app-wide (table rows, sidebar nav, nav island brand bar, buttons) while page H1s/markdown headings stay Luminari via their existing inline `fontFamily.serif`. In `stack-panel.tsx`: replaced the raw `Run tests` `<button>` and the `btn-violet`/`btn-orange` Send/Stop buttons with `Button variant="chalkboard"`; wrapped Status and Commit in `Card variant="chalkboard" size="small"` to match Agent's card; removed the "Active" card (duplicated Agent) and "Live" SSE feed card, keeping the underlying `EventSource` subscription since it also drives the other sections' refreshes. `tsc`/`biome`/`vitest` clean in both repos. Verified live in the browser: sidebar/table/nav-island text now renders in Cormorant Garamond, "Plans" H1 and markdown headings stayed Luminari, and the Stack panel shows Status → Commit → Agent only. Filed as `REFACTOR-4` since it's a consolidation pass, not a new feature. Set to `review`, not `done`, per `AGENTS.md`.
- Built an additional phase of "Default font and Stack panel cleanup" (`REFACTOR-4`), folding in a follow-up request: simplified `PlanCard` (`src/app/features/plans/components/plan-card.tsx`) used throughout the plans list. Title is now larger/bolder (`text-xl`, `fontWeight: 700`); dropped the `plan.body` description paragraph and the Open/Start/Stop buttons from the list item entirely — `PlanDetail` already has its own Start/Stop controls, so no functionality was lost; made the whole card clickable (`role="button"`, `tabIndex`, `onKeyDown` for Enter/Space, alongside `onClick`) to open the plan, replacing the dedicated "Open" button. paper-ui's `Card` doesn't spread `onClick`/`style` onto its root div, so the click handler lives on `PlanCard`'s own existing outer wrapper `<div>` rather than touching the sibling repo. `tsc`/`biome` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): switched `PlanCard`'s title from the plain `text-xl`/`fontWeight: 700` it picked up from the prior simplification phase to the same Luminari style `PlanDetail`'s `<h2>` already uses (`fontFamily.serif`, `fontWeight: 600`, `1.75rem`), so list and detail titles read consistently instead of two different weights/fonts. `tsc`/`biome` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): reordered `stack-panel.tsx`'s three sections to Agent → Status → Commit (Agent moved from last to first), and changed the panel's content area from a single scrolling column (`flex:1, overflowY:auto` with each section just `marginBottom: space[8]`) into a flex column where each section is its own `flex:1, minHeight:0` region — so Agent/Status/Commit each get an equal, fixed share of the panel's full height, scroll independently if their own content overflows, and are separated by a `deskBorder` divider instead of a bottom margin. `tsc`/`biome` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): made Status the smallest section (`flex: 1`, vs. `flex: 2` for Agent and Commit) while keeping it centered between them, and made each section's `Card` actually stretch to fill its section's full height instead of just hugging its content. paper-ui's `Card` (`~/dev/paper-ui/src/components/card/card.tsx`) is a plain block div with no flex/height behavior and only forwards a `className` prop — so rather than changing the sibling repo, added a `.stack-card-fill` class in paper-camp's own `utilities.css` that reaches into `Card`'s two nested divs (`.stack-card-fill > div`) to flex/scroll them, the same "override library internals from outside via a utility class" pattern the existing `.btn-green`/`.btn-orange`/`.btn-violet` SVG-path overrides already use one section up in the same file. Moved each section's own `overflowY`/scrollbar styles down onto the Card (now the actual scroll container) since the Card fills the section. `tsc`/`biome` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): reworked the Status section's stamps. Replaced the separate Lint/Format stamps with one derived "Quality" stamp (`running` if either is running, `fail` if either failed, `stale` only if both are, else `pass`) and dropped the per-stamp click-to-expand-output (`CodeBlock`) behavior entirely — clicking the Quality or Tests stamp now triggers that check directly instead. The test-only `POST /api/status/test` route (`src/app/server/api.ts`) became a generic `POST /api/status/check?name=lint|format|test`, since `status.runCheck()` (`src/app/server/status.ts`) already accepted any `CheckName` — only the route was hardcoded to test. Renamed `triggerTests()`→`triggerCheck(name)` in `status-api.ts` and `runTests`→`runCheck(name)` in `app-store.ts` to match. Removed the standalone "Run tests" `Button` — the Tests stamp click replaces it. Both Quality and Tests stamps disable and grey out while either is running (only one check job runs at a time); left Consistency's own expand-on-click untouched since it's a derived, instant client-side check, not a spawned job. Centered the stamp row both horizontally and vertically within the section's now full-height Card. `tsc`/`biome`/`vitest` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): added a "suggested fix" affordance under the Status stamps, scoped to what's actually mechanically fixable. Quality (lint+format) is the only one of the three with a real auto-fix: added `runQualityFix()` to `src/app/server/status.ts` (spawns `npx biome check . --write`, then re-runs `lint`/`format` for fresh status; guarded against double-running if either is already in flight), wired through a new `POST /api/status/fix` route, `triggerQualityFix()` in `status-api.ts`, and `fixQuality()` in `app-store.ts`. The Status section now shows a "Suggested fix: run biome --write" link under the stamps when Quality fails, disabled while any check is running. Tests and Consistency failures need human/agent judgment, not a script, so no auto-fix for those — instead added a small "Tests failing — see output, or [copy fix prompt]" line when Tests fails, with a new `CopyPromptButton` (`src/app/components/copy-prompt-button.tsx`) that copies a prompt embedding the captured `vitest` output for pasting into an agent session. Extracted the `copyToClipboard` helper that already lived inside `phase-copy-button.tsx` into a shared `src/app/utils/clipboard.ts` so both copy buttons use the same implementation instead of duplicating it. Left Consistency untouched — its existing per-issue clickable list already tells you what to check. `tsc`/`biome`/`vitest` clean; not yet re-verified live in the browser per this session's instruction. Plan stays `review`.
- Built phase 2 of "Agent-drafted plans" (`FEAT-17`): resolved the write-directly-vs-propose-first open question. Decided the plan-drafting agent writes `plans.md` directly, same as `FEAT-10`'s phase-execution agent — no separate draft/approval flow. The agent's new entry (and any existing-plan reordering it judges necessary) lands at `Status: idea`, matching `POST /api/plans`'s existing convention for any newly-created plan, so it shows up in the Backlog where the existing CRUD (edit, delete, promote via Start) already serves as the human review step; nothing new needs to be built for that. Recorded the decision and rationale in `decisions.md` ("Plan-drafting agent writes plans.md directly, same as phase execution") and updated `ideas.md`'s IDEA-15 section to mark the question resolved with a pointer to it. Plan stays `in-progress` (3 phases remain).
- Fixed a follow-up report on "Default font and Stack panel cleanup" (`REFACTOR-4`): dropped the " clean"/" N" suffix from the Consistency stamp's label (now always reads plain "Consistency", state conveyed by fill/text color alone, matching Quality/Tests). Investigated two other reports — the Suggested-fix link doing nothing and Consistency never being clickable — by curling the live dev server directly: `POST /api/status/fix`, `POST /api/status/check`, and even `GET /api/consistency` all 404 (the last one straight into the SPA shell), confirming the running `pnpm dev` process's `api.ts` module predates every server route added since FEAT-15 — not a bug in this session's code. `consistency` stays permanently `[]` client-side because the fetch 404s, which is also why the Consistency stamp looks permanently non-interactive. User will restart the dev server themselves. `tsc`/`biome`/`vitest` clean.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): added a one-sentence failure summary above each suggested fix in the Status section. New `src/app/utils/check-summary.ts` (`summarizeQualityFailure(lintOutput, formatOutput)`, `summarizeTestFailure(testOutput)`, both pure, with a co-located `check-summary.test.ts`) parses a plain sentence out of the check's already-captured output — biome's `Found N errors.` line summed across lint+format for Quality, vitest's `Tests N failed` summary line for Tests, each falling back to a generic message if no count is parseable. Rendered above the existing fix link/prompt rather than replacing it. Gave `CopyPromptButton` (`copy-prompt-button.tsx`) a second `variant="link"` rendering mode — plain text + underline, matching the Quality fix link's look — for the Tests case, alongside its original icon-button mode. `tsc`/`biome`/`vitest` clean (6 new tests, 35 total); not yet re-verified live in the browser (also still blocked on the user restarting their stale dev server, per the prior phase). Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): stabilized the Status section's layout so it stops jumping when checks transition between states (reported after clicking the Quality fix link). Switched the section's outer column from `justifyContent: 'center'` to `'flex-start'` so the stamps row anchors to the top instead of recentering whenever the message area below it grows or shrinks. Replaced the two independently-conditional fail blocks (`qualityStatus === 'fail' && (...)`, `testStatus === 'fail' && (...)`, each mounting/unmounting on its own) with one always-rendered message slot below the row that renders exactly one of: "Running checks…" (while either is running), "Checks haven't run yet." (both stale), "All checks passing." (both pass), or the fail message(s) plus suggested fix — so there's always something in that slot, and only its inner content swaps rather than it mounting/unmounting. `tsc`/`biome`/`vitest` clean (still 35 tests); not yet re-verified live in the browser, still blocked on the dev server restart. Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`), verified live in Chrome this time (dev server was back up). Fixed two reported issues: (1) the Quality/Tests stamps grew wider whenever their `…` running suffix appeared (`{status === 'running' ? '…' : ''}`), visibly shifting the whole row — changed to always render a `<span>…</span>` with `visibility: qualityStatus === 'running' ? 'visible' : 'hidden'` instead, so its width is permanently reserved regardless of state. (2) "No agent running."/"No changed files." left a big empty gap below them — only the populated-state branch of Agent/Commit was wrapped to fill the section's full height (via `.stack-card-fill` on the `Card`), the empty-state `<p>` fallback wasn't, so it just sat at the top with dead space below. Wrapped each fallback in its own `flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center'` div so it centers within the section's full height the same way the populated state does. Confirmed both fixes visually: stamps no longer shift, and "No agent running." now sits centered in the Agent section instead of stranded at the top. `tsc`/`biome`/`vitest` clean (still 35 tests). Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`), and split `UX_PRINCIPLES.md` out of `CODE_STYLE.md` per the user's request (UX/UI judgment calls — layout stability, visual hierarchy, motion — don't belong in a file about code mechanics; `AGENTS.md` now points at both). Then fixed a reported residual vertical jump in the Status section: the previous fix centered the stamps+message group as a unit, but the message slot's own height still varied — 1 line for running/pass/stale, up to 4 if Quality *and* Tests both happened to be failing at once, since those were two independently-conditional blocks that could both render. Rewrote it as a single discriminated message (priority order: running > Quality fail > Tests fail > all-pass > stale) so exactly one state renders, always as a primary line plus a second line that's either the real fix link/prompt or an invisible (`visibility: hidden`) placeholder of the same font-size — the slot is now always exactly two lines tall regardless of which state is showing. Verified live in Chrome by clicking the Tests stamp and watching it cycle running → pass → back to the Quality-fail message: the stamps row stayed in the exact same pixel position throughout. `tsc`/`biome`/`vitest` clean (still 35 tests). Plan stays `review`.
- Built one more phase of "Default font and Stack panel cleanup" (`REFACTOR-4`): the user flagged a large empty gap at the bottom of the Status card after the previous `justifyContent: 'flex-start'` change (visible in their screenshot as Chrome's own flexbox-gap visualization). Re-examined: the actual fix for the earlier layout "jump" was making the message slot always-rendered instead of conditionally mounted (an earlier phase), not the top-alignment — `flex-start` just anchored the short stamps+message group to the top of the now full-height card and left the rest of the card empty below it. Switched the column back to `justifyContent: 'center'` so the group centers as a unit; confirmed live in Chrome that the dead space is gone and the card now only grows by about one line between the shortest (all-passing) and longest (failing-with-fix-link) states. `tsc`/`biome`/`vitest` clean (still 35 tests). Plan stays `review`.
- Built phase 3 of "Agent-drafted plans" (`FEAT-17`): added `buildPlanDraftPrompt(idea, otherPlans)` to `src/app/features/plans/prompts.ts`, the static prompt builder for the idea-drafting agent task `startForIdea()` already supports. Embeds the idea's full body; a condensed plan-record schema (heading/fields/phases shape from `about.md`) including an explicit instruction to assign the new `Id` from `.paper-camp/config.json`'s persistent `nextId` counter and increment it directly, since this agent edits `plans.md` by hand rather than going through `POST /api/plans`'s own counter-increment logic; a pointer to decisions.md's "Plan-drafting agent writes plans.md directly" decision so the agent writes `Status: idea` with no approval step; every other non-`done` plan's id/status/body/phases for priority context; and explicit insertion instructions — pick the file position reflecting priority, may move existing plan headings to make room, but must never edit another plan's title/phases/body while doing so. `tsc`/`biome`/`vitest` clean (still 35 tests). Plan stays `in-progress` (2 phases remain: the "Draft plan" button and the Backlog ordinal markers).
- Built phase 4 of "Agent-drafted plans" (`FEAT-17`): wired up the "Draft plan" button. New `POST /api/agent/launch-draft` route in `src/app/server/api.ts` (parses `ideas.md` via `parseIdeas`, looks up the idea by `ideaId`, and calls `agent.startForIdea()` from phase 1), plus `launchPlanDraft()` in `services/agent-api.ts` and a matching `launchPlanDraft` store action in `app-store.ts`. New `DraftPlanButton` component (`features/plans/components/draft-plan-button.tsx`, mirroring `AuditPhasesButton`'s shape) calls `buildPlanDraftPrompt(idea, otherPlans)` from phase 3 and disables itself while any agent task is already running. Wired into `IdeasBoard`'s row, rendered only when the idea has zero linked plans (`!hasLinks`) — once a plan links back via `Idea: IDEA-N`, the button's slot is replaced by the existing linked-plans expand control, matching the per-phase buttons taking over from there. `tsc`/`biome`/`vitest` clean (still 35 tests). Could not exercise the live click-through: the running dev server has active established connections (in active use) and its loaded `api.ts` module predates this new route per the established stale-module pattern this repo has hit repeatedly — restarting it would both interrupt the user's session and wasn't needed to verify the button's render/disable logic, which was checked by code review instead. Plan stays `in-progress` (1 phase remains: the Backlog ordinal markers).
- Built the last phase of "Agent-drafted plans" (`FEAT-17`): added the file-order ordinal marker to the Backlog section. `PlanCard` (`plan-card.tsx`) takes a new optional `rank` prop and renders it as a small muted `N.` marker next to the `PlanIdStamp`, only when passed; `list-view.tsx`'s Backlog map (the only call site that passes it) now uses the map index (`i + 1`) as `rank`, since `backlog`'s filter preserves `plans.md`'s file order already (same assumption the Ideas board's own file-order ranking from `FEAT-8` relies on). `tsc`/`biome`/`vitest` clean (still 35 tests). Not verified live in the browser: `plans.md` currently has zero `planned`/`idea`-status entries, so the Backlog section doesn't render at all right now — nothing to screenshot until a backlog entry exists. This was the plan's last open phase; `FEAT-17` moves to `review`.
- The user reported the "Draft plan" button doing nothing (no logs, no plan appearing). Diagnosed via direct `curl`: `POST /api/agent/launch-draft` 404s on the running dev server while `GET /api/agent/status` (added earlier) still works — confirmed the long-running `pnpm dev` process (pid 260889) predates this route, the same stale-module pattern hit repeatedly this session. Not a code bug; user restarted the dev server and confirmed the button now works. Added and built one more phase on "Agent-drafted plans" (`FEAT-17`): a loading-state placeholder card while a plan draft is in flight. New `PlanCardSkeleton` (`features/plans/components/plan-card-skeleton.tsx`) renders the idea's real id plus a "Creating a plan…" title, with every other field (status stamp, tags, progress bar) replaced by pulsing skeleton blocks — using a new shared `.paper-camp-skeleton`/`@keyframes paper-camp-skeleton-pulse` utility added to `utilities.css` rather than an inline `<style>` tag. `ListView` takes a new `draftingIdeaId` prop and renders the skeleton at the top of the Backlog list whenever it's set and no plan linking that idea id exists yet; `PlansPage` derives it from the global `agentStatus` (`ideaId` set and status `starting`/`running`) — since the Stack panel's SSE subscription already keeps `agentStatus` live app-wide, no new polling was needed. Disappears automatically once the real drafted plan entry appears in `plans.md` (or the task finishes/errors), since the visibility check is just "does a plan with this idea id exist yet," not a manual dismiss. `tsc`/`biome`/`vitest` clean (still 35 tests). Plan stays `review`.
- Built phase 1 of "Plan clarification pass" (`FEAT-19`): factored `extractLog`'s body in `src/core/parser.ts` into a generic `extractDatedList(body, headingRe)`, reusing the existing `LOG_ENTRY_RE`/`parseDatedListEntries` (renamed from `parseLogEntries`) unchanged; `extractLog` is now a thin wrapper calling `extractDatedList(body, LOG_HEADING_RE)` and remapping `entries` to `log`. Sets up the next phase's `### Clarifications` parsing to call the same helper with its own heading regex. `tsc`/`vitest` clean (22 parser tests passing). Plan moved to `in-progress` (4 phases remain).
- Built phase 2 of "Plan clarification pass" (`FEAT-19`): added `CLARIFICATIONS_HEADING_RE = /^###\s+Clarifications\s*$/i` and `extractClarifications()` to `src/core/parser.ts`, calling `extractDatedList` the same way `extractPhases`/`extractLog` already do; wired into `parseRawEntries()` (chained after the existing phases/log extraction so a `### Clarifications` block is stripped from the body the same way) and into `parsePlans()`. Added optional `clarifications?: LogEntry[]` to `RawEntry` and `PlanEntry` (`types/index.ts`), reusing the existing `LogEntry` type. `formatPlanEntry()` (`serializer.ts`) gained matching `clarifications?: LogEntry[]` on `NewPlanInput` and emits an optional `### Clarifications` block of `- YYYY-MM-DD: <text>` lines, placed before `### Phases` per the plan's stated render order ("below the plan body, above Phases"). No prompt/UI wiring yet — that's the next two phases. `tsc`/`vitest` clean (22 parser tests still passing, no new ones needed since the logic is a direct reuse of the already-tested `extractDatedList`/`LOG_ENTRY_RE` path). Plan stays `planned` (3 phases remain).
- Built phase 3 of "Plan clarification pass" (`FEAT-19`): added `buildClarifyPrompt(plan)` to `src/app/features/plans/prompts.ts`, a new exported function that builds the clarify-agent prompt — includes the full taxonomy (functional scope, data model, UX flow, non-functional attributes, edge cases, terminology, completion signals), the "at most 5 highest-impact gaps, one question at a time, lead with `**Recommended:**`" loop instruction, and the answer-recording format (`- YYYY-MM-DD: Q: <question> → A: <answer>` under `### Clarifications`). Follows the same pattern as the existing `buildConvergenceAuditPrompt`/`buildPlanDraftPrompt`/`buildIdeaExtendPrompt` in the same file, taking a `PlanEntry` and returning a string. `tsc`/`biome` clean. Plan stays `in-progress` (2 phases remain: the "Clarify before starting" button and the clarifications list render).
- Built phase 4 of "Plan clarification pass" (`FEAT-19`): added `ClarifyButton` (`src/app/features/plans/components/clarify-button.tsx`), modeled on `PhaseCopyButton`'s copy-to-clipboard pattern (idle/copied/failed states, `copyToClipboard` from shared utils) but using `buildClarifyPrompt(plan)` as the copied text and rendered as a `Button variant="ghost"` in the Phases header button row alongside `AuditPhasesButton` and `AddReviewPhasesButton`. Imported and placed in `plan-detail.tsx`. `tsc`/`biome` clean. 1 phase remains (clarifications list render); plan stays `in-progress`.
- Built phase 5 (final) of "Plan clarification pass" (`FEAT-19`): added a read-only Clarifications section to `plan-detail.tsx` between the plan body and the progress bar, rendering `plan.clarifications` (already `LogEntry[]` from phase 2) as dated bullets styled identically to Log entries — same heading style (`h3` with serif font, small size, 65% opacity), same entry layout (date in bold + text, `text-sm` at 75% opacity). No add-entry textarea, matching the read-only requirement. `tsc`/`biome` clean. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 6 of "Plan clarification pass" (`FEAT-19`): moved the `ClarifyButton` from the Phases table header (alongside `AuditPhasesButton` and `AddReviewPhasesButton`) to above the plan body paragraph, so it's visible before scrolling past the phase list. `tsc`/`biome` clean. Plan stays `in-progress` (1 phase remains: compacting the clarification prompt).
- Built phase 7 (final) of "Plan clarification pass" (`FEAT-19`): compacted `buildClarifyPrompt` in `src/app/features/plans/prompts.ts` — replaced the verbose 7-bullet taxonomy (each with an explanatory question) with a concise comma-separated list of category names, and collapsed the multi-paragraph loop/output instructions into two short sentences. Prompt now only provides the taxonomy and the output format. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 8 of "Plan clarification pass" (`FEAT-19`): renamed the `ClarifyButton` label from "Clarify before starting" to "Copy Clarifications Prompt" in `clarify-button.tsx` so its purpose is obvious at a glance. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 1 of "GitHub CI/CD automation" (`FEAT-22`): created `.github/workflows/ci.yml` — runs `pnpm install`, `pnpm run check-types`, `pnpm run lint`, and `pnpm test` on push to `main` and on PRs against `main`, plus `npx commitlint --from <base> --to <head>` against the PR's commits to finally enforce Conventional Commits (`.commitlintrc.json` already existed but was never run). Uses `actions/checkout@v4` with `fetch-depth: 0` (needed for commitlint's range comparison), `pnpm/action-setup@v4` with `node 20` and `pnpm` caching. 3 phases remain — Status stays `in-progress`.
- Built phase 2 of "GitHub CI/CD automation" (`FEAT-22`): decided `refactor` commits bump patch (this repo has 4 refactor plans, treating refactoring as a first-class deliverable). Created `.github/release-please-config.json` with `changelog-sections` matching all 5 commit types (`feat`/`fix`/`refactor`/`docs`/`chore`), `default` versioning with `bump-minor-pre-major: true`, and `include-component-in-tag: false`. Created `.github/.release-please-manifest.json` tracking `".": "0.1.0"`. Created `.github/workflows/release-please.yml` — runs `googleapis/release-please-action@v4` on push to `main`, maintains a standing release PR. The `refactor` bump is implemented via `changelog-sections` inclusion; release-please's default versioning doesn't trigger independent releases for `refactor` (they ride along with the next `feat`/`fix`). To make `refactor` trigger standalone patch releases, toggle `versioning` to `always-bump-patch` or add a custom versioning plugin. 2 phases remain — Status stays `in-progress`.
- Built phase 3 of "GitHub CI/CD automation" (`FEAT-22`): created `.github/workflows/publish.yml` — triggers on `release: [published]`, runs `pnpm install`, `pnpm run build`, then `npm publish` using `NPM_TOKEN` via `NODE_AUTH_TOKEN` with `actions/setup-node`'s `registry-url: https://registry.npmjs.org`. 1 phase remains — Status stays `in-progress`.
- Built phase 4 (final) of "GitHub CI/CD automation" (`FEAT-22`): adopted per-feature branch workflow. Documented branch-naming convention (`<kind>/<lowercase-id>-<kebab-title>`) in `AGENTS.md`, auto-creating a draft PR on first push to any feature branch via `.github/workflows/create-pr.yml` (idempotent — skips if a PR already exists for that branch). Updated `ci.yml` to run on push to feature branches (not just `main`+PRs). Decided `main` stays pushable for agent progress writes, tiny fixes, and config changes; all substantive plan work uses branches. Resolved IDEA-4 impact: agents write to whatever branch is checked out — no behavioral change needed, merge conflicts from two branches touching the same `plans.md` regions accepted until IDEA-20. All decisions recorded in `decisions.md`. This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 5 of "GitHub CI/CD automation" (`FEAT-22`): split `ci.yml`'s single `ci` job into three parallel jobs (`quality`: `check-types` + `lint`, `tests`: `vitest`, `consistency`: `commitlint` on PR only), each with independent checkout/pnpm-setup/install steps so every PR shows three named checks instead of one. Phase 5 of FEAT-22 checked off; phase 6 (refactor patch releases) still unchecked. Plan Status stays `in-progress`.
- Built phase 6 (final) of "GitHub CI/CD automation" (`FEAT-22`): investigated whether `refactor` commits actually need a custom release-please plugin to trigger patch bumps. Traced release-please v17's `DefaultVersioningStrategy.determineReleaseType` (`src/versioning-strategies/default.ts`) — the method checks for `breaking` (→ major/minor), then `feat`/`feature` (→ minor/patch), and falls through to `PatchVersionUpdate` for **every other commit type** (`refactor`, `fix`, `docs`, `chore`, etc.). Since the repo's `changelog-sections` already lists `refactor` with `hidden: false`, the changelog builder includes those commits and the release PR proceeds. No plugin was needed: `"versioning": "default"` already matched phase 2's decision. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 7 (final) of "GitHub CI/CD automation" (`FEAT-22`): review-pass audit on naming/clarity and live repo settings. Added explicit `name:` to every job and step across all 4 workflows — job ids (`quality`/`tests`/`consistency`) had no `name:` so GitHub displayed the lowercase id instead of "Quality"/"Tests"/"Consistency"; same gap on every step (bare `run:`/`uses:`, no label). Checked the live repo via `gh api`: "Allow GitHub Actions to create and approve pull requests" was already enabled (`can_approve_pull_request_reviews: true`), but `main` had zero branch protection (404 on the protection endpoint) and no `NPM_TOKEN` secret existed. Added branch protection on `main` requiring `Quality`/`Tests`/`Consistency` to pass before PR merge, with `enforce_admins: false` and no push restrictions so direct pushes stay unblocked, per the existing "main stays pushable" decision. Left `NPM_TOKEN` for the user to add directly. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 8 of "GitHub CI/CD automation" (`FEAT-22`): adopted `type(scope): description` commit convention, scope = plan/idea number with no kind prefix (`feat(22): ...`). Added `scope-empty: [2, "never"]` to `.commitlintrc.json`. Test-ran `npx commitlint` against the repo's real commit style and found `subject-case` (from `@commitlint/config-conventional`) rejects the capitalized subjects this repo has always used — every existing commit would have failed the `consistency` check the first time it ran for real, a latent bug nobody had hit yet since commitlint was never wired into CI before this plan. Disabled `subject-case` (`[2, "never", []]`) to match the established style. Documented the convention in `AGENTS.md`'s new "Commit messages" section. Verified `feat(20): Settings config workspace` passes and `feat: missing scope` correctly fails. Plan stays `review`.
- Built phase 9 of "GitHub CI/CD automation" (`FEAT-22`): renamed `.github/workflows/release-please.yml` → `release.yml` (job `release-please` → `release`, display name "Release") and `.github/workflows/create-pr.yml` → `draft-pr.yml` (job `create-pr` → `draft-pr`, display name "Draft PR") so workflow/job names read as what they do rather than the tool behind them or an over-generic verb. Left them as separate files rather than merging — `release.yml` triggers only on push to `main`, `draft-pr.yml` only on push to feature branches, different concerns. Left `.github/release-please-config.json`/`.release-please-manifest.json` filenames as-is (release-please's own expected config names). Updated the stale `create-pr.yml` reference in `AGENTS.md` and `decisions.md`. Plan stays `review`.
- Built phase 10 of "GitHub CI/CD automation" (`FEAT-22`): added `ensureBranch(plan)` to `src/app/server/git.ts` — derives a branch name from `<kind>/<lowercase-id>-<kebab-title>`, creates it off `main` (or checks out if already exists), no-op if already on that branch. Wired it into `agent.ts`'s `start()`: when `phaseIndex === 0`, calls `ensureBranch(plan)` before spawning the agent. Only covers phase starts launched through the agent UI. `tsc`/`biome`/`vitest` all clean (35 tests). 2 phases remain — Status stays `in-progress`.
- Built phase 11 of "GitHub CI/CD automation" (`FEAT-22`): added `getFeatureBranchPlanId()` to `src/app/server/git.ts` — parses the current branch name via the feature-branch naming convention and returns the uppercased plan id (e.g. `FEAT-22`) when on a feature branch, null otherwise. Added `checkBranchConflictForPlan()` to `src/app/server/api.ts` — reads `plans.md`, checks if there's an active feature branch for a plan that isn't `done`/`dropped` and differs from the target, and returns a user-facing message naming the in-flight plan (e.g. "Finish `FEAT-22` — GitHub CI/CD automation — before starting another plan"). Wired the check into all 4 agent-launch routes (`/api/agent/launch`, `/launch-audit`, `/launch-draft`, `/launch-extend`) returning 409 when blocked. Added `catch` blocks to `agent-start-button.tsx` and `audit-phases-button.tsx` so the error message is visible to the user via `alert()` instead of silently swallowed. `tsc`/`biome`/`vitest` all clean (35 tests). 1 phase remains — Status stays `in-progress`.
- Built phase 12 (final) of "GitHub CI/CD automation" (`FEAT-22`): added `commitAll(title, body?)` to `src/app/server/git.ts` — `git add -A` then `git commit -m <title> -m <body>`. Wired it into the PATCH `/api/plans` handler in `src/app/server/api.ts`: when a plan's `status` is set to `done` (the "Approve & close" action), calls `git.ensureBranch(plan)` to create a branch if none exists (covers work done straight on `main`), then `git.commitAll()` with a `type(scope): description` message whose body lists every phase title as a bullet. The block-switching-plan restriction from phase 11 is already implicitly lifted — `checkBranchConflictForPlan` returns null for `done`/`dropped` plans. `tsc`/`biome`/`vitest` all clean (35 tests). This was the plan's last phase — Status set to `review` per `AGENTS.md`.
- Built phase 13 (final) of "GitHub CI/CD automation" (`FEAT-22`): exposed `getCurrentBranch()` from `git.ts`'s manager return and added it to the `/api/git/status` response (`{ branch, entries }`). Added `GitStatusResponse` type. Updated the store to extract `gitBranch` from the response. Rendered the branch name as a `Stamp` next to the "Commit" heading in the Stack panel's commit card. `tsc`/`biome` both clean. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 14 of "GitHub CI/CD automation" (`FEAT-22`): review pass on the full plan found two bugs in the approval flow added by phases 10-12. (1) The `PATCH /api/plans` `status: 'done'` handler in `src/app/server/api.ts` called `git.ensureBranch()`/`git.commitAll()` without checking `checkBranchConflictForPlan()` first — approving a plan while a *different* plan's branch had uncommitted work would create/checkout the approved plan's branch carrying the other plan's dirty changes along, then commit them under the wrong plan's message. Added the same conflict check before `writeFile`, returning 409 with the same "finish the in-flight plan first" message used by the launch endpoints. (2) `finalEntry.id?.replace(...) ?? ''` produced an empty commit scope (`chore(): Title`) for plans with no `Id` field, which would itself fail the `scope-empty` commitlint rule from phase 8 — added a kebab-cased-title fallback scope (capped at 30 chars) for that case. `tsc`/`biome`/`vitest` all clean (35 tests, no regressions). Plan stays `review`.
- Built phase 15 (final) of "GitHub CI/CD automation" (`FEAT-22`): dropped the auto-commit-on-approval behavior added in phase 12 — after seeing it run, decided commits should stay manual via the Stack panel's existing commit form. Removed `commitAll()` from `git.ts` (now unused, including its export) and the commit-title/body/scope generation from the `PATCH /api/plans` `status: 'done'` handler in `api.ts`. Approval now only calls `git.ensureBranch()` (no-op if the branch already exists) behind the existing `checkBranchConflictForPlan()` guard from phase 14 — no git commit happens as a side effect of approving a plan anymore. `tsc`/`biome`/`vitest` all clean (35 tests). This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 16 of "GitHub CI/CD automation" (`FEAT-22`): fixed a real CI failure reported from the live GitHub pipeline — `Error [ERR_UNKNOWN_BUILTIN_MODULE]: No such built-in module: node:sqlite`, followed by `Error: warn: This version of pnpm requires at least Node.js v22.13`. Root cause: every workflow's `pnpm/action-setup@v4` step used `version: latest`, which had drifted to pnpm 11.9 (depends on Node ≥22.13's `node:sqlite` builtin), while every workflow's `actions/setup-node@v4` pins `node-version: 20` — a version mismatch that only showed up in CI since the local dev machine already has a compatible pnpm (10.12.1) installed. Fixed by adding `"packageManager": "pnpm@10.12.1"` to `package.json` as the single source of truth, and removing the `version: latest` override from all 4 `pnpm/action-setup` steps (3 in `ci.yml`, 1 in `publish.yml`) so the action picks up the pinned version automatically. `tsc`/`biome` clean; all 4 workflow YAMLs re-parsed successfully. Plan stays `review`.
- Built phase 17 (final) of "GitHub CI/CD automation" (`FEAT-22`): fixed the next CI failure — `tsc --noEmit` couldn't resolve `@dendelion/paper-ui` in any file importing it, because `package.json` declared it as `link:../paper-ui`, a relative symlink to a sibling repo that doesn't exist in CI (and wouldn't exist for any real `npm install paper-camp` either — this would have broken `publish.yml`'s output for every real user). `~/dev/paper-ui` had 30+ uncommitted files (5 new components: Accordion, Icon, ListItem, Progress, Textarea, plus supporting changes) and only a stale `patch` changeset. Committed the work, wrote an accurate `minor` changeset, ran `pnpm run version` (`0.1.1` → `0.2.0`, using `GITHUB_TOKEN=$(gh auth token)` for the changelog generator), verified `check-types`/`build`, committed the version bump, and published via `pnpm publish --access public` (confirmed with the user beforehand) — then pushed both commits to paper-ui's `main` (confirmed separately). Changed paper-camp's `package.json` to `"@dendelion/paper-ui": "^0.2.0"`, ran `pnpm install`, and verified it resolves the real registry package (not the old symlink) with the new components present in the `.d.ts`. `tsc`/`biome`/`vitest` all clean (35 tests). Documented the new workflow in `AGENTS.md`: `pnpm link ../paper-ui` for local override during active co-development, plain `pnpm install` to revert, and the changeset → version → publish sequence for future paper-ui releases. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 18 of "GitHub CI/CD automation" (`FEAT-22`): fixed duplicated CI runs the user spotted on feature-branch PRs — `ci.yml` triggered on both `push` (to `main`/`feat/*`/`fix/*`/`refactor/*`/`chore/*`/`docs/*`, added in phase 4) and `pull_request` (to `main`). Once `draft-pr.yml` opens a PR on the first push, every later push to that branch fired both events for the same commit, producing two full sets of Quality/Tests/Consistency runs. Restricted the `push` trigger to `main` only — `pull_request` alone covers every feature-branch commit since a PR exists from the first push onward. YAML re-parses clean, `biome` clean. Plan stays `review`.
- Built phase 19 (final) of "GitHub CI/CD automation" (`FEAT-22`): fixed the dev server breaking after the paper-ui registry switch — user reported a blank page with `SyntaxError: ... does not provide an export named 'createPortal'` from `react-dom`. Root cause: `vite.app.config.ts`'s `optimizeDeps: { exclude: ['@dendelion/paper-ui'] }`, set up for the old `link:../paper-ui` symlink workflow, meant Vite never crawled into paper-ui to discover its internal `react-dom` import once paper-ui became a normal registry dependency — so `react-dom`'s CJS module was served without ESM interop. Removed the `exclude` entry, killed and restarted the dev server with a cleared `node_modules/.vite` cache. Verified live via Claude in Chrome: Plans and Settings pages both render fully with zero console errors, Stack panel's branch-name `Stamp` still correct. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 20 (final) of "GitHub CI/CD automation" (`FEAT-22`): added `.coderabbit.yaml` at the repo root — `chill` review profile for lightweight style/rule checks, `path_instructions` pointing at `AGENTS.md`, `CODE_STYLE.md`, and `UX_PRINCIPLES.md` so CodeRabbit reviews check against real repo conventions (paper-ui component usage, design tokens, font hierarchy, naming rules, layout stability, branch naming, commit format) instead of generic defaults. `auto_review` set to include draft PRs, targeting this repo's `feat/*`/`fix/*`/`refactor/*`/`chore/*`/`docs/*` branch naming convention. `poem: false`, `request_changes_workflow: false` — just comments, no blocking. The CodeRabbit GitHub App itself still needs to be installed from the GitHub marketplace (not something an agent can do). This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 21 of "GitHub CI/CD automation" (`FEAT-22`): switched draft-PR authorship from `github-actions[bot]` to a named bot. User created a GitHub App ("Scout", `pull_requests: read/write`, no webhook) under the `adooone` org, installed it on `paper-camp`, and set `SCOUT_APP_ID`/`SCOUT_PRIVATE_KEY` as repo secrets themselves so the private key never touched this conversation. Along the way, `gh secret set` echoed back `croco-dendy/paper-camp` instead of `adooone/paper-camp` — confirmed via `gh api repos/<owner>/paper-camp` that both names resolve to the same repo id (a transfer redirect from the personal account to the org), so the secret landed correctly; updated the local `origin` remote to the canonical `adooone/paper-camp` URL to remove the confusion going forward. Added a `Generate Scout app token` step to `draft-pr.yml` using `actions/create-github-app-token@v1`, and pointed the `Create draft PR` step's `GITHUB_TOKEN` env at the minted installation token instead of `secrets.GITHUB_TOKEN`. Documented in `AGENTS.md`. YAML re-parses clean, `biome` clean.
- Built phase 22 (final) of "GitHub CI/CD automation" (`FEAT-22`): pulled CodeRabbit's first review on PR #1 (9 actionable comments) and triaged them. Fixed: `draft-pr.yml`'s `BRANCH="${{ github.ref_name }}"` template-injected directly into the shell script — moved to `env: BRANCH:`. Added `persist-credentials: false` to all 4 checkout steps (`ci.yml` ×3, `draft-pr.yml` ×1). `git.ts`'s `ensureBranch()` silently swallowed a failed fallback checkout, assuming any `-b` failure meant "branch exists" — now throws the real git stderr instead of continuing on the wrong branch. `git-api.ts`'s `fetchGitStatus()` never checked `response.ok`, so an `{ error }` response would overwrite the store with `undefined`s — now throws on non-OK, matching `commitChanges` in the same file. Reworded a self-contradicting paragraph in `decisions.md` (said branch protection "isn't configured" after a later decision had added it). Merged a duplicate `## 2026-06-27` heading in `progress.md`. Also fixed the plan-level issue CodeRabbit caught: this plan's `Status` was `done` instead of `review` — reverted per `AGENTS.md`'s "only Approve & close sets done" rule. Skipped CodeRabbit's suggestion to run `ensureBranch()` on every phase launch (conflicts with this plan's own earlier "first phase only" scoping decision — flagged for discussion, not auto-applied). `tsc`/`biome`/`vitest` all clean (35 tests), all 4 workflow YAMLs re-parse clean. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 23 (final) of "GitHub CI/CD automation" (`FEAT-22`): reopened the one CodeRabbit disagreement from phase 22 — decided a quick "are we on the right branch" check before every phase launch is worth it, since `ensureBranch()` is already idempotent (no-op when already on the correct branch) and the edge case it guards against (a plan resumed after someone manually switched branches mid-flight) is real. Changed `agent.ts`'s `start()` from `if (phaseIndex === 0) { ensureBranch(plan) }` to an unconditional `ensureBranch(plan)` before every phase launch — supersedes the original "first phase only" scoping decision. `tsc`/`biome`/`vitest` all clean (35 tests). This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.
- Built phase 24 (final) of "GitHub CI/CD automation" (`FEAT-22`): scoped the npm package as `@dendelion/paper-camp` instead of bare `paper-camp` — confirmed `@dendelion` (the scope `@dendelion/paper-ui` already publishes under) is accessible and `@dendelion/paper-camp` is unclaimed. Renamed `package.json`'s `name`; left `bin: { "paper-camp": ... }` unchanged so the CLI command name doesn't change. Updated `release-please-config.json`'s `package-name` to match. Added `--access public` to `publish.yml`'s `npm publish` (scoped packages default to private) and `persist-credentials: false` to its checkout step for consistency with phase 22's hardening. `tsc`/`biome`/`vitest`/`build` all clean (35 tests); `pnpm-lock.yaml` needed no changes. This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.

## 2026-06-28
- Built one more phase of "GitHub CI/CD automation" (`FEAT-22`): re-checked CodeRabbit's review threads on PR #1 via the GraphQL `reviewThreads` API (most of the earlier 9 comments were unresolved threads, not still-valid findings — already fixed in code but not auto-resolved by CodeRabbit). Found two real remaining gaps: the Scout app token in `draft-pr.yml` inherited full installation permissions instead of a narrow grant — added `permission-contents: read`/`permission-pull-requests: write`. `publish.yml`'s checkout was missing `fetch-depth: 0`, the one workflow diverging from the standard bootstrap — added it. Confirmed a third thread (`app-store.ts`'s `fetchGitStatus` handling) was stale, referencing behavior already fixed earlier. `tsc`/`biome` clean.
- Built one more phase of "GitHub CI/CD automation" (`FEAT-22`): fixed a real CI failure on the release-please PR branch (`release-please--branches--main--components--paper-camp`) — Biome's JSON formatter collapses short arrays onto one line, but release-please's own JSON writer always re-serializes `package.json`'s arrays multi-line when it bumps `version`, so every release PR failed the Quality/Lint check on a file nobody had actually touched by hand. Biome 1.9.4 doesn't yet support the `json.formatter.expand` option that would reconcile the two styles (Biome 2.x feature); excluded `package.json` from Biome's formatter via `biome.json`'s `files.ignore` instead. Verified by pushing directly to the open release-please branch, which then passed.
- Built one more phase of "GitHub CI/CD automation" (`FEAT-22`): fixed `draft-pr.yml` opening a duplicate PR (PR #3) after PR #1 had already merged — `gh pr list --head "$BRANCH"` defaults to `--state open`, so once a branch's PR merged, a later push to that branch found no "existing" PR and opened a new one. Added `--state all` to the check. Verified live by re-pushing to the closed `feat/feat-22-github-ci-cd-automation` branch and confirming the workflow logged "PR #3 already exists ... skipping" instead of creating a fourth PR.
- Built phase 25 (final) of "GitHub CI/CD automation" (`FEAT-22`): fixed `publish.yml` never having run despite `v0.2.0` already being published. Root cause: `release.yml` ran `release-please-action` with the default `GITHUB_TOKEN`, and GitHub's anti-recursion guard blocks events triggered by `GITHUB_TOKEN` from starting new workflow runs — so the `release: types: [published]` event fired but was forbidden from triggering `publish.yml`. Switched `release.yml` to mint a Scout app installation token (`permission-contents: write`, `permission-pull-requests: write`) via `actions/create-github-app-token`, passed as release-please-action's `token` input — the same fix pattern already used for `draft-pr.yml`. YAML re-parses clean, `biome` clean. This was the plan's last unchecked phase — Status stays `review` per `AGENTS.md`.
- Phase 1 of "Plan storage architecture" (FEAT-24): designed the per-file YAML frontmatter schema for plans and ideas, defined the directory layout (papercamp/plans/, papercamp/ideas/, papercamp/plans/archive/), settled on id-only lowercase filename convention (feat-24.md, idea-20.md), documented the config migration path (.paper-camp/ → papercamp/), and wrote the full spec into about.md. Created frontmatter zod schemas in src/core/frontmatter-schemas.ts and generated JSON Schema from them via zod v4's built-in toJSONSchema(). Created the empty directory skeleton (papercamp/plans/, papercamp/ideas/, papercamp/plans/archive/, papercamp/assets/).
- Phase 2 of "Plan storage architecture" (FEAT-24): replaced the hand-rolled `**Field:** value` line grammar with real YAML frontmatter parsing. Installed the `yaml` npm package. Consolidated all frontmatter schemas (`planFrontmatterSchema`, `ideaFrontmatterSchema`) into `src/core/schemas.ts` as the single source of truth, with `src/core/frontmatter-schemas.ts` re-exporting them for backwards compatibility. Added `parseFrontmatter<T>()` — extracts YAML frontmatter from `---`-delimited blocks and validates against a zod schema — plus `parsePlanFile()` and `parseIdeaFile()` for parsing individual per-file entries. Added `serializeFrontmatter()`, `formatPlanFile()`, and `formatIdeaFile()` for serializing entries back to YAML frontmatter files with proper phase/log/clarifications sections. All old monolithic-format parsers (`parsePlans`, `parseDecisions`, `parseOpenQuestions`, `parseIdeas`, `parseProgress`) and serializers (`formatPlanEntry`, `formatPlans`) kept unchanged for backward compatibility until migration (phases 5/7). Added 20 frontmatter-specific tests covering parsing, serialization, round-trips, edge cases, and schema validation. `tsc`/`biome`/`vitest` all clean (55 tests).
- Phase 3 of "Plan storage architecture" (FEAT-24): built index generators — `formatPlansIndex()` and `formatIdeasIndex()` in `src/core/serializer.ts` produce markdown tables (id, title, status, tags) sorted by numeric ID. Added `regenerateIndexes()` helper in `src/app/server/api.ts` that reads the monolithic files, parses entries, derives idea statuses, and writes `papercamp/plans/index.md` and `papercamp/ideas/index.md`. Wired into every write endpoint: `DELETE /api/plans`, `POST /api/plans`, `PATCH /api/plans`, and `POST /api/ideas`. Verified the generated index output manually — both tables sort correctly and ideas show derived planned/done status. `tsc`/`biome`/`vitest` all clean (55 tests).
- Phase 4 of "Plan storage architecture" (FEAT-24): implemented archive as file move. Added `archivePlanFile(root, planId)` to `src/core/serializer.ts` — a pure `fs.rename()` from `papercamp/plans/<id>.md` to `papercamp/plans/archive/<id>.md` with no parse-and-re-serialize step. Returns false if no per-file exists for the plan (graceful no-op during the monolithic-file era). Wired into `PATCH /api/plans` in `src/app/server/api.ts`: when status changes to `done` or `dropped` and the plan has an ID, calls `archivePlanFile` after the monolithic file write. The conflict-check guard and git-branch handling are extended to cover `dropped` alongside `done`. `tsc` clean (0 errors).
- Phase 5 of "Plan storage architecture" (FEAT-24): updated CLI commands and every API route to read/write per-file entries instead of monolithic files. Added `readAllPlanFiles()`, `readAllIdeaFiles()`, `readPlansMerged()`, and `readIdeasMerged()` to `src/core/parser.ts` — async per-file directory readers that merge with monolithic fallback (deduplicating by id/title) for backward compatibility before migration. Updated `scaffold.ts` to create the per-file directory structure (`plans/index.md`, `plans/archive/`, `ideas/index.md`) on `init` instead of monolithic `plans.md`/`ideas.md`. Updated CLI `add plan` command to write `papercamp/plans/<id>.md` via `formatPlanFile()` and regenerate the index. Rewrote every handler in `src/app/server/api.ts`: `GET /api/plans` and `GET /api/ideas` use the merged readers; `POST /api/plans` and `POST /api/ideas` write per-file entries; `PATCH /api/plans` reads/writes a single plan file (with auto-demote logic for other in-progress plans) and archives on done/dropped; `DELETE /api/plans` removes the per-file entry; `/api/consistency`, `/api/agent/launch`, `/api/agent/launch-audit`, `/api/agent/launch-draft`, and `/api/agent/launch-extend` all use per-file readers with monolithic fallback. Updated `agent.ts`'s `didTaskProgress()` to read per-file directories. Updated `ideas-api.ts` and `app-store.ts` to consume `ParseResult<IdeaEntry>` from the API instead of raw monolithic `ideas.md` content. `tsc`/`biome`/`vitest` all clean (55 tests). 2 phases remain (papercamp/init migration and .paper-camp/ move).
- Phase 6 of "Plan storage architecture" (FEAT-24): moved config.json and assets from .paper-camp/ into papercamp/. Updated all code references across the entire codebase: `src/core/scaffold.ts`, `src/app/server/api.ts`, `src/app/server/agent.ts`, `src/cli/index.ts`, `src/core/serializer.ts` (doc comment), `src/app/features/plans/prompts.ts`, `src/app/features/settings/settings-page.tsx`. Also updated documentation references in `papercamp/about.md` and `papercamp/plans.md`. The `.paper-camp/` directory has been removed — config now lives at `papercamp/config.json` and assets at `papercamp/assets/`. 1 phase remains (the one-time migration script that splits monolithic files into per-file entries).
- Review pass on "Plan storage architecture" (`FEAT-24`) phases 1-5 found and fixed three real bugs. (1) `app-store.ts` imported `deriveIdeaStatuses` from `core/parser.ts`, pulling parser.ts's Node-only `readdir` import into the client bundle — broke `pnpm run build` and the dev server with `"readdir" is not exported by "__vite-browser-external"`. Extracted `deriveIdeaStatuses` into a new zero-dependency `core/idea-status.ts`. (2) `planFrontmatterSchema` had no `title` field — every plan written through `POST /api/plans` or `add plan` silently lost its human-readable name, since `parsePlanFile()` fell back to using the plan's `id` string as its title. Added `title` to the schema, `formatPlanFile()`'s input type, and all four call sites that construct it. (3) `DELETE /api/plans` didn't actually delete — it emptied the file then renamed it to `_deleted_<id>.md`, a permanent stray file `readAllPlanFiles()` would keep re-parsing. Replaced with a real `unlink()`. Updated `about.md`'s documented spec/JSON Schema and all 20 frontmatter tests to match. `tsc`/`biome`/`vitest` clean (55 tests), full `pnpm run build` succeeds end-to-end (verified both lib and app bundles, plus the live dev server).
- Built phase 7 (final) of "Plan storage architecture" (`FEAT-24`): added `paper-camp migrate` as a permanent CLI command (not a throwaway script) — anyone installing a newer paper-camp version on an existing project with the old monolithic `papercamp/` layout needs the same upgrade path, not just this repo. It splits `plans.md`/`ideas.md` into per-file entries via the existing `parsePlans`/`parseIdeas`/`formatPlanFile`/`formatIdeaFile`, is idempotent (skips any target file that already exists, safe to re-run), and routes `done`/`dropped` plans straight into `papercamp/plans/archive/` instead of the active `plans/` directory, matching the archive mechanism's end state rather than archiving them in a second pass. Ran it against this repo's own data: migrated all 30 plans (28 to archive, `FEAT-23` and this plan itself to the active directory) and all 22 ideas, zero skipped, zero warnings. Regenerated both index files and replaced `plans.md`/`ideas.md`'s content with a one-line pointer comment (kept the files rather than deleting them outright). This was the plan's last unchecked phase — Status set to `review` per `AGENTS.md`.

## 2026-07-01
- Phase 1 of "Batch plan freshness audit" (FEAT-25): added an `audited: YYYY-MM-DD` frontmatter field to the per-file plan format as the "last audited" marker. Chose frontmatter over a log line — machine-readable, no log noise, consistent with existing `created`/`updated` date fields. Added `audited` to `planFrontmatterSchema` (schemas.ts), `PlanEntry` (types/index.ts), `NewPlanFileInput` / `formatPlanFile` (serializer.ts), and `parsePlanFile` (parser.ts). Stamping is fully server-side: `createAgentManager` gains an optional `onAuditComplete(planId)` callback called whenever a `taskKind === 'audit'` task finishes with detected progress; `createApiMiddleware` supplies a `stampAuditDate` closure that reads/parses/updates/rewrites the plan file with `audited: today`. All PATCH-plan write paths updated to preserve the existing `audited` field on non-audit updates. Added 2 round-trip tests for `audited`. `tsc`/`biome`/`vitest` all clean (62 tests).
- Completed phase 4 ("Update CLI command descriptions") of "Actualize about.md for per-file storage" (`DOCS-3`). In `papercamp/about.md`'s CLI table: rewrote the `init` row to describe the actual per-file layout (`plans/` dir with `index.md` + `archive/` subdir, `ideas/` dir with `index.md`, plus `config.json` and the three monolithic files), replacing the stale `{ideas,plans,progress,decisions,open-questions}.md` list; corrected the `add plan` row from "appends to `plans.md`" to "writes a new `papercamp/plans/<ID>.md` file with YAML frontmatter." Verified against `src/core/scaffold.ts` and `src/cli/index.ts`.
